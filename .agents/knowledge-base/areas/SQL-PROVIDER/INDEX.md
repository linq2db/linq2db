---
area: SQL-PROVIDER
kind: area-index
sources: [code]
confidence: high
last_verified: 2026-07-05
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
coverage_tier_1: 4/4
coverage_tier_2: 48/48
---

# SQL-PROVIDER

Provider-agnostic SQL emission and statement-level optimization. Lives under `Source/LinqToDB/Internal/SqlProvider/` (the build/optimize machinery -- `LinqToDB.Internal.SqlProvider` namespace) and `Source/LinqToDB/Sql/` (the legacy public `Sql.*` static class -- namespace `LinqToDB`). Together these define the contract between the [SQL-AST](../SQL-AST/INDEX.md) (input) and the per-provider subclasses under `Internal/DataProvider/<Provider>/` (output).

The two anchor abstractions are `ISqlBuilder` (turns a `SqlStatement` into provider-specific SQL text) and `ISqlOptimizer` (rewrites a `SqlStatement` into a shape the builder can emit). Each in-tree provider ships exactly one concrete `<Provider>SqlBuilder : BasicSqlBuilder` and one `<Provider>SqlOptimizer : BasicSqlOptimizer`, registered via `IDataProvider.CreateSqlBuilder` (`Source/LinqToDB/Internal/DataProvider/DataProviderBase.cs:123`). The dialect override mechanism is plain virtual-method inheritance -- `BasicSqlBuilder` exposes ~130 `protected virtual` hooks (sentence prefixes like `BuildXxx`, `IsXxxSupported`, `GetXxxName`) that subclasses override surgically.

`Source/LinqToDB/Sql/` is the user-facing surface: `public static partial class Sql` (`Source/LinqToDB/Sql/Sql.cs:27`) hosts every server-side helper a query can call (`Sql.AsSql`, `Sql.AsNullable`, `Sql.Between`, `Sql.Collate`, `Sql.GroupBy.Rollup`, the `Sql.DateAdd` family, the `Sql.Row(...)` constructors, the `Sql.Window.*` fluent window-function DSL, ...) plus the attribute hierarchy used by user code to declare custom translations (`Sql.ExpressionAttribute`, `Sql.FunctionAttribute`, `Sql.ExtensionAttribute`, `Sql.PropertyAttribute`, `Sql.TableFunctionAttribute`, `Sql.TableExpressionAttribute`, `Sql.QueryExtensionAttribute`).

## Key types

- **`ISqlBuilder`** (`Source/LinqToDB/Internal/SqlProvider/ISqlBuilder.cs:11`) -- the emission contract. Entry point is `BuildSql(commandNumber, statement, sb, optimizationContext, aliases, nullabilityContext, startIndent)` (`ISqlBuilder.cs:15`); auxiliary surface includes `BuildObjectName`, `Convert(sb, value, ConvertType)`, `BuildExpression`, `GetIdentityExpression`, `PrintParameters`, `ApplyQueryHints`, `GetReserveSequenceValuesSql`, `GetMaxValueSql`. Carries five state properties: `Name`, `MappingSchema`, `StringBuilder`, `SqlProviderFlags`, `TableIDs`. The `CommandCount(SqlStatement)` overload lets a provider emit multiple SQL statements per logical operation.

- **`ISqlOptimizer`** (`Source/LinqToDB/Internal/SqlProvider/ISqlOptimizer.cs:7`) -- the rewrite contract. Three entry points: `Finalize`, `IsParameterDependent`, `ConvertSkipTake`. Two factory methods (`CreateOptimizerVisitor`, `CreateConvertVisitor`) and `CreateSqlExpressionFactory` let providers swap visitor subclasses.

- **`BasicSqlBuilder`** (`Source/LinqToDB/Internal/SqlProvider/BasicSqlBuilder.cs:26`) -- abstract default implementation, ~5000 lines (grew from ~4650), partial across `BasicSqlBuilder.cs` and `BasicSqlBuilder.Merge.cs`. Walks the AST clause-by-clause via the `Step` enum (`BasicSqlBuilder.cs:4048`). Statement dispatch happens in `BuildSqlImpl` (`BasicSqlBuilder.cs:367`) -- a switch over `Statement.QueryType` calls `BuildSelectQuery`, `BuildDeleteQuery`, `BuildUpdateQuery`, `BuildInsertQuery`, `BuildInsertOrUpdateQuery`, `BuildCreateTableStatement`, etc. Critical hook properties: `OpenParens`, `Comma`, `InlineComma`, `IsRecursiveCteKeywordRequired`, `IsCteColumnListSupported`, `SupportsMaterializedCteHint`, `WrapJoinCondition`, `IsNestedJoinSupported`, `CteFirst`, `IsOverRequiredWithinGroup`, `IsUpsertUpdateWhereAfterSet` (new, see delta). Hooks for sub-clauses to override: `BuildSelectClause`, `BuildColumns`, `BuildFromClause`, etc. plus `protected abstract ISqlBuilder CreateSqlBuilder()` -- every provider overrides this to spawn a fresh same-typed builder for sub-queries.

  **Delta (PR #5504):** `BasicSqlBuilder` gains the concat-emission subsystem:
  - `ConcatBuildStyle` enum (`BasicSqlBuilder.cs:3874`) -- three values: `Plus` (`a + b`; SQL Server pre-2025, SqlCe, Access), `Pipes` (`a || b`; ANSI standard -- PostgreSQL, Oracle, SQLite, DB2, Firebird, Informix, SAP HANA, DuckDB, Sybase ASE, SQL Server 2025+), `Function` (`CONCAT(a, b, c)`; MySQL, ClickHouse).
  - `protected virtual ConcatBuildStyle ConcatStyle` (`BasicSqlBuilder.cs:3888`) -- defaults to `Plus`. Provider subclasses override to select their style.
  - `protected virtual string ConcatFunctionName` (`BasicSqlBuilder.cs:3894`) -- defaults to `"CONCAT"`; overridable for case-sensitive dialects (e.g. ClickHouse `concat`).
  - `protected virtual void BuildSqlConcatExpression(SqlConcatExpression element)` (`BasicSqlBuilder.cs:3896`) -- the primary emission hook; dispatches on `ConcatStyle` to either `BuildSqlConcatOperatorChain` (for `Plus`/`Pipes`) or `BuildSqlConcatFunctionCall` (for `Function`). Override this only when none of the three styles fits.
  - Expression dispatch: `QueryElementType.SqlConcat` routes to `BuildSqlConcatExpression` at `BasicSqlBuilder.cs:3243-3245`.

  **Delta (this run -- PR #5468, window functions):** `BasicSqlBuilder.BuildSqlExtendedFunction` (`BasicSqlBuilder.cs:3538`) is extended to emit the full window-function grammar the new `Sql.Window.*` DSL (see below) can produce:
  - `WindowNullsPlacement` enum (`BasicSqlBuilder.cs:3513`) -- `AfterClose` (`FUNC(args) IGNORE NULLS`; Oracle, SQL Server 2022+, Informix), `AfterLastArgument` (`FUNC(..., expr IGNORE NULLS)`; DuckDB), `StringArgument` (`FUNC(args, 'IGNORE NULLS')`; DB2).
  - `protected virtual WindowNullsPlacement GetWindowNullsPlacement(SqlExtendedFunction)` (`BasicSqlBuilder.cs:3527`) and `protected virtual bool WindowFunctionRespectNullsRequired(SqlExtendedFunction)` (`BasicSqlBuilder.cs:3536`) -- the latter flips the *default* null-treatment so providers where IGNORE NULLS is the implicit default (ClickHouse FIRST_VALUE/LAST_VALUE/NTH_VALUE) can force an explicit `RESPECT NULLS` token; ClickHouse LEAD/LAG rejects the token entirely and returns `false`.
  - `extendedFunction.KeepClause` emission (`BasicSqlBuilder.cs:3613-3618`) -- Oracle `KEEP (DENSE_RANK FIRST|LAST ORDER BY ...)` via `SqlKeepClause.KeepType`.
  - Frame `Exclusion` emission (`BasicSqlBuilder.cs:3712-3721`) -- `SqlFrameClause.FrameExclusionKind.{CurrentRow,Group,Ties}` -> `EXCLUDE CURRENT ROW` / `EXCLUDE GROUP` / `EXCLUDE TIES`.
  - Frame boundary direction bugfix: both `frame.Start`/`frame.End` offsets now check `IsPreceding` before emitting ` PRECEDING`/` FOLLOWING` (previously `End` always emitted `FOLLOWING` unconditionally).
  - `extendedFunction.FromPosition == Sql.From.Last` emits `FROM LAST` (the SQL-default `FROM FIRST` is omitted).
  - The `OVER (...)` emission gate now also fires on `extendedFunction.IsWindowFunction` alone (previously required a non-null Partition/Order/Frame), so a bare `OVER ()` window call with no clauses still renders correctly.

  **Delta (this run -- PR #5482, fluent Upsert):** `protected virtual bool IsUpsertUpdateWhereAfterSet => false` (`BasicSqlBuilder.cs:120`) controls where an `Upsert(...).Update(v => v.When(...))` predicate lands in MERGE-shaped emission: `false` (SQL Server / PostgreSQL 15+) emits `WHEN MATCHED AND <cond> THEN UPDATE SET ...`; `true` (Oracle) emits `WHEN MATCHED THEN UPDATE SET ... WHERE <cond>`. Both `BuildInsertOrUpdateQueryAsMerge` (`BasicSqlBuilder.cs:1365,1381`) and `BuildInsertOrUpdateQueryAsOnConflictUpdateOrNothing` (PostgreSQL `ON CONFLICT ... DO UPDATE SET ... WHERE`) honor the predicate. `BuildInsertOrUpdateQueryAsUpdateInsert` (the `IF @@ROWCOUNT = 0`-style emitter) cannot represent an UPDATE-branch predicate at all and throws `ErrorHelper.Error_Internal_UpdateInsertEmitter_CannotEmitUpdatePredicate` if one is present -- callers on such providers must route through the 3-query `SetIfExistsUpdateElseInsert` orchestration instead.

  **Delta (this run -- PR #5630, DISTINCT ON):** `protected virtual void BuildDistinctModifier(SelectQuery)` (`BasicSqlBuilder.cs:800`) replaces the previous hard-coded `" DISTINCT"` append in `BuildSelectClause`; providers supporting `SqlProviderFlags.IsDistinctOnSupported` (PostgreSQL, DuckDB) override it and call the new `protected void BuildDistinctOnExpressions(SelectQuery)` (`BasicSqlBuilder.cs:811`) to render `DISTINCT ON (expr, ...)`, reusing the `ORDER BY` expression renderer so the PostgreSQL/DuckDB "ON list must be a prefix of ORDER BY" rule is satisfied by construction.

  **Delta (this run -- PR #5450, CTE-as-table generalization):** `SqlCteTable`/`SqlCteTableField` now participate in code paths previously hard-typed to `SqlTable`/`SqlField`: a new `BuildExpression` case for `QueryElementType.SqlCteTableField` (`BasicSqlBuilder.cs:3159`), `BuildAnchor`'s four table-lookup branches switched from `SqlField { Table: ... }` to `SqlFieldBase { NamedTable: ... }`, `FindTable` returns `ISqlNamedTable?` instead of `SqlTable?`, `GetTableAlias` splits the `SqlTable`/`SqlCteTable` cases (previously fell through the same `SqlTable` cast), and `GetPhysicalTableName` gains an explicit `SqlCteTable` branch building the CTE name via `ConvertType.NameToCteName`. Enables the eager-loading CTE-union strategy (see `BasicSqlOptimizer` delta and `SqlProviderFlags.MaxColumnCount`) to route setters/joins/dedup through CTE-backed sources the same way it does for ordinary tables.

  Also: `SupportsColumnAliasesInScalarSource` (`BasicSqlBuilder.Merge.cs`, defaults to `SupportsColumnAliasesInSource`) and `protected virtual void BuildEmptyValuesFrom()` (`BasicSqlBuilder.cs:2109`) split the "does this provider accept a column-alias list after a VALUES-source alias" question from "...after a scalar/raw-SQL-source alias", and let a provider supply a one-row dummy `FROM` for an empty-values `SELECT ... WHERE 1 = 0` independent of `FakeTable`.

- **`BasicSqlBuilder<T>`** (`Source/LinqToDB/Internal/SqlProvider/BasicSqlBuilder{T}.cs:8`) -- typed-options facade. `T : DataProviderOptions<T>, IOptionSet, new()`.

- **`BasicSqlOptimizer`** (`Source/LinqToDB/Internal/SqlProvider/BasicSqlOptimizer.cs:22`) -- abstract default optimizer, ~2600 lines (grew from ~2260). The `Finalize` pipeline (line 52) runs in fixed order: `FixEmptySelect` -> `FinalizeCte` -> `SqlNullsOrderingLoweringVisitor.LowerNullsOrdering` (when `!IsNullsOrderingSupported`) -> `OptimizeQueries` -> conditional `JoinsOptimizer.Optimize` -> `FinalizeInsert` -> `FinalizeSelect` -> `FixSetOperationValues` -> `FinalizeStatement` (provider hook). The `Alternative Builders` region (line 830) holds reusable rewrite helpers (`GetAlternativeDelete`, `RemoveUpdateTableIfPossible`, `NeedsEnvelopingForUpdate`, `ReplaceTakeSkipWithRowNumber`).

  **Delta (this run -- PR #5450, ISqlNamedTable generalization):** the UPDATE-correction path is retyped from `SqlTable` to `ISqlNamedTable` throughout: `FindTableSource` (`BasicSqlOptimizer.cs:113,139`), `IsCompatibleForUpdate` (`:182`), `ApplyUpdateTableComparison` (`:232,254`), `BasicCorrectUpdate` (`:265`, doc comment explains: "`SqlCteTable` participates here too... the `Clone` extension preserves the concrete table type, so `SqlCteTable` flows through the same correction path as `SqlTable`"), `RemoveUpdateTableIfPossible` (`:885,890`), `CloneQuery`/`CloneTable`/`CorrectReplaceTree` (`:959-998`, `CorrectReplaceTree` also gains an `SqlCteTableField` branch). Net effect: `.Update()` against a CTE-backed source (needed by the new CTE-union eager-loading strategy) goes through the same table-clone/key-comparison machinery as an ordinary table UPDATE, instead of failing the type check.

  **Delta (this run -- issue #5413 / PR #5584, Oracle correlated UPDATE ROW fix):** `FoldJoinConditionAndDetach` (`BasicSqlOptimizer.cs:1656`) now rebases any `SqlColumn` owned by the fold target before concatenating the join condition into `target.Where` -- previously a projected correlation-key column reference could dangle once the caller cleared that column from the target's projection. `FlattenRowConstructors`'s SelectQuery-RHS branch now clones the update subquery **per output column** (`clonedSubquery.Select.Columns.Clear(); ...Add(keepColumn)`) instead of sharing one subquery reference across all row-setter columns -- the shared-subquery form was silently eliminated by the optimizer as "unused" (its columns lived in the SET list, not the SELECT), leaving setters pointing at columns from a source no longer in the FROM.

  **Delta (this run -- PR #5630, DISTINCT ON):** `SeparateDistinctFromPagination` (`BasicSqlOptimizer.cs:2343`) excludes `q.Select.IsDistinctOn` queries from its DISTINCT-vs-pagination split (`:2348`) -- a `DISTINCT ON` query is not equivalent to a plain `DISTINCT` for this rewrite's purposes.

- **`SqlProviderFlags`** (`Source/LinqToDB/Internal/SqlProvider/SqlProviderFlags.cs:19`) -- `[DataContract]`, ~78 mutable bool/enum fields. `[DataMember(Order = N)]` is mandatory because remoting (gRPC/WCF/HTTP) round-trips this object. The `CustomFlags` set is a string-keyed escape hatch.

  **Delta (NULLS ordering flags):** Two new `[DataMember]` fields: `IsNullsOrderingSupported` (Order=69, bool, default false, `SqlProviderFlags.cs:604`) -- native NULLS FIRST/NULLS LAST vs CASE emulation; and `DefaultNullsOrdering` (Order=70, NullsDefaultOrdering, default Unknown, `SqlProviderFlags.cs:614`) -- natural null placement in ORDER BY. ABI-additive: old payloads deserialize with safe defaults.

  **Delta (this run -- PRs #5482/#5450/#5630):** six more `[DataMember]` fields, all ABI-additive:
  - `IsInsertOrUpdateWithPredicateSupported` (Order=71, default `true`, `:615`) -- native single-statement InsertOrUpdate can carry an `Upsert(...).Update(v => v.When(...))` predicate; `false` for MySQL/MariaDB (`ON DUPLICATE KEY UPDATE` has no WHERE), Sybase ASE and SQL Server 2005 (both `IF @@ROWCOUNT = 0`-emulated).
  - `IsUpsertWithMergeLoweringSupported` (Order=72, default `true`, `:631`) -- provider can render the synthesized two-branch MERGE shape for bulk/non-PK/conditional Upserts; `false` for SAP HANA (its MERGE dialect lacks `WHEN NOT MATCHED THEN INSERT`).
  - `IsUpsertMergeWithPredicateSupported` (Order=73, default `true`, `:648`) -- MERGE dialect supports `WHEN [NOT] MATCHED AND <cond>`; `false` for Firebird 2.5 (pre-dates Firebird 3's conditional MATCHED syntax).
  - `IsInsertOrUpdateRequiresAlignedBranches` (Order=74, default `false`, `:665`) -- `true` forces fallback to the alternative UPDATE-then-INSERT emulation whenever INSERT/UPDATE branches diverge in per-column expressions, because the provider's native single-statement form shares one VALUES list across both branches; set for SAP HANA (`UPSERT ... WITH PRIMARY KEY`).
  - `MaxColumnCount` (Order=76, `int`, default `0` = unlimited, `:675`) -- caps the SELECT-list width linq2db will build for a CteUnion eager-loading query before falling back to individual preamble queries (PR #5450).
  - `IsDistinctOnSupported` (Order=75, default `false`, `:702`) -- native `SELECT DISTINCT ON (expr, ...)`; PostgreSQL and DuckDB set it. When `false`, `DistinctBy` falls back to `ROW_NUMBER()`/`OUTER APPLY` emulation (owned by EXPR-TRANS).

  All six fields are folded into `GetHashCode`/`Equals` (`SqlProviderFlags.cs:795-802`, `876-883`).

- **`OptimizationContext`** (`Source/LinqToDB/Internal/SqlProvider/OptimizationContext.cs:13`) -- bag passed from LINQ pipeline into SQL builder. Carries `EvaluationContext`, `DataOptions`, `SqlProviderFlags`, `MappingSchema`, the two visitor instances, a `Factory` (`ISqlExpressionFactory`), parameter-dedup map, `SuggestDynamicParameter` cache. The two `OptimizeAndConvert*` methods drive the two-phase optimize-then-convert pipeline.

  **Delta (this run):** `AddParameter`'s inline name-collision normalization is extracted into a new public helper, `public string? NormalizeParameterName(string? name)` (`OptimizationContext.cs:102`), so other call sites needing a collision-free parameter name (e.g. Upsert-lowering code outside `AddParameter`'s own path) can reuse the same normalizer instance instead of duplicating the `_parametersNormalizer ??= _parametersNormalizerFactory()` pattern.

- **`SqlExpressionOptimizerVisitor`** -- provider-agnostic shape-preserving rewrites. ~2185 lines.

  **Delta (PR #5502):** `VisitSqlCoalesceExpression` (`SqlExpressionOptimizerVisitor.cs:1416`) gains two rewrites in one pass: (1) **Nested coalesce flattening** -- when an operand is itself a `SqlCoalesceExpression` (possibly in `SqlNullabilityExpression`), its children are inlined, eliminating `COALESCE(COALESCE(a,b),c)` -> `COALESCE(a,b,c)` shapes. (2) **Early-termination** -- when a mid-chain expression is provably non-nullable and not the last, the chain is truncated. Eliminates spurious `COALESCE(agg_subquery, 0)` patterns.

- **`SqlExpressionConvertVisitor`** -- provider-specific `IQueryElement` transforms. ~2105 lines.

  **Delta (PR #5504):** Gains the concat-lowering subsystem:
  - `protected virtual bool ConcatRequiresExplicitStringCast` (`SqlExpressionConvertVisitor.cs:1259`) -- when `true` (default), wraps every non-string operand in an explicit `CAST(... AS VARCHAR(N))` before the concat chain. Needed for `+` providers (SQL Server pre-2025, SqlCe, Access) where SQL data-type precedence would otherwise coerce string operands to the non-string type. `||` and `CONCAT(...)` providers override this to `false`. **Sybase ASE exception** (`SqlExpressionConvertVisitor.cs:1289`): Sybase ASE emits `||` but keeps `ConcatRequiresExplicitStringCast = true` because ASE requires explicit `convert()` for non-character operands.
  - `public virtual ISqlExpression ConvertConcat(SqlConcatExpression element)` (`SqlExpressionConvertVisitor.cs:1261`) -- the per-element lowering called from `VisitSqlConcatExpression`. Pipeline: (1) single-operand identity pass; (2) `FlattenNestedConcat` -- flattens same-`PreserveNull`-semantic nested `SqlConcatExpression` children (addresses the `string + string + string` arrives as `Concat(Concat(a,b),c)` pattern from `TranslateBinaryStringConcat`); (3) per-operand cast-to-string when `ConcatRequiresExplicitStringCast`; (4) per-nullable-operand `Coalesce(item, '')` wrap when `!element.PreserveNull` (null-as-empty semantic). Idempotence is guarded by `IsConcatCoalesceWrap` which detects four shape variants the optimizer can produce from the same logical wrap.
  - `VisitSqlConcatExpression` (`SqlExpressionConvertVisitor.cs:1170`) -- override that calls `ConvertConcat` and re-enters `Visit` on any replacement.
  - `RemoveNullValues(SqlCoalesceExpression element)` (`SqlExpressionConvertVisitor.cs:1247`, protected) -- strips NULL-literal operands before `ConvertCoalesce` folds. Prevents Informix `Nvl(x, NULL)` / Access `IIF(x IS NULL, NULL, x)` artifacts (issue #5531).

  **Delta (this run -- PR #5450):** `WrapBooleanExpression`'s field-wrap condition (`SqlExpressionConvertVisitor.cs:1755`) now also matches `QueryElementType.SqlCteTableField`, so a bare boolean CTE-field reference gets the same `= 1`/parenthesization treatment as a plain `SqlField`/`Column`.

- **`JoinsOptimizer`** -- sealed; runs only when `LinqOptions.OptimizeJoins` is on. Two passes: `RemoveUnusedLeftJoins`, `RemoveDuplicateJoins`. Static `UnnestJoins` flattens nested JOINs.

  **Delta (this run -- PR #5450):** `SqlCteTableField` now participates in join optimization the same way `SqlField`/`SqlColumn` do: `GetFieldSourceID` (`JoinsOptimizer.cs:346`), `GetUnderlyingFieldOrColumn` (`:406`), `CanRemoveLeftJoin`'s used-outside-join check (`:232`), and `IsSimilarFields` (`JoinsOptimizer.RemoveDuplicates.cs:599-605`, compares by `ReferenceEquals(cteField1.CteField, cteField2.CteField)`) all gained an `SqlCteTableField` branch, so duplicate-join removal and unused-left-join removal work correctly when the join target is a CTE rather than a table. `UndoNestedJoins` (`JoinsOptimizer.cs:159`) changed signature -- now takes `SqlQueryColumnNestingCorrector`, `SqlQueryColumnOptimizerVisitor`, and `MappingSchema`, and returns `IQueryElement` (previously `void`): after re-nesting, it also re-runs column optimization so a newly-minted, unreferenced derived table projects `SELECT 1` rather than `SELECT *` (which "could fail if there are columns with same name" from the joined tables it wraps).

- **`SqlExpressionFactory`** -- minimal default `ISqlExpressionFactory`.

- **`ConvertType`** -- enum tagging *what kind of identifier* an `ISqlBuilder.Convert` call is escaping.

- **`Sql.cs`** (`Source/LinqToDB/Sql/Sql.cs:27`) -- the public `Sql` static class. Namespace: `LinqToDB`, not `LinqToDB.Sql`. The class is `partial` and split across ~29 files by topic (was 28 -- see `WindowFunctions.FeatureMatrix.md` below, a doc not a partial-class file, plus `Sql.Window.cs` growing from a stub into the full implementation).

  **Delta (this run -- PR #5648, UUIDv7):** `public static Guid NewGuid7()` (`Sql.cs:266`) returns an RFC 9562 version-7 (time-ordered) GUID. Translated server-side on providers with a native UUIDv7 function (PostgreSQL 18+, ClickHouse, DuckDB, MariaDB) via EXPR-TRANS; other providers evaluate client-side. On `NET9_0_OR_GREATER` delegates to `Guid.CreateVersion7()`; older TFMs (`net462`/`netstandard2.0`/`net8.0`) use a hand-rolled `CreateVersion7Fallback()` that fills 16 random bytes, overlays a millisecond Unix timestamp in the top 48 bits, sets the version/variant nibbles, and reassembles via the endianness-aware `Guid(int, short, short, byte...)` constructor.

  **Delta (this run -- PR #5604, PreferClientCalculation fix):** `Sql.AsNullable<T>` (`Sql.cs:75`) and `Sql.ToNullable<T>` (`Sql.cs:98`) lost their `[Expression("{0}", ...)]` attributes. Both are now translator-driven (via `SqlFunctionsMemberTranslatorBase`) so they always stay server-side; the attribute previously let `LinqOptions.PreferClientCalculation`'s projection client-calculation pull the call client-side, which collapsed a SQL `NULL` to `default(T)` before the nullability widen instead of preserving `null`.

- **`Sql.ExpressionAttribute`** + subclasses (`Sql.PropertyAttribute`, `Sql.FunctionAttribute`, `Sql.ExtensionAttribute`, `Sql.TableFunctionAttribute`, `Sql.TableExpressionAttribute`) -- foundational attribute hierarchy.

- **`Sql.QueryExtensionAttribute`** + **`Sql.QueryExtensionScope`** -- declare query-shape extensions. Four built-in builders: `HintExtensionBuilder`, `HintWithParameterExtensionBuilder`, `HintWithParametersExtensionBuilder`, `HintWithFormatParametersExtensionBuilder`.

- **`Sql.SqlID`** -- typed `(SqlIDType, string)` pair for hint extensions to refer back to a tagged table.

- **`Sql.AggregateModifier` / `From` / `Nulls` / `NullsPosition`** -- nominal enums for window-function clauses. `Sql.Nulls.{Ignore,Respect}` and `Sql.From.{First,Last}` are consumed directly by `BasicSqlBuilder.BuildSqlExtendedFunction`'s new NULLS-treatment/FROM-position emission (see delta above).

- **`Sql.IsNullableType`** -- nullability propagation rules for `[Expression]` decorations.

- **`WindowFunctionBuilder`** (`Source/LinqToDB/Sql/Sql.Window.cs:27`) -- **new in this run (PR #5468, "Window Functions: new Sql.Window API")**. Public `static class` implementing the full `Sql.Window.*` fluent DSL, entered via `Sql.IWindowFunction`/the `Sql.Window` static accessor (declared elsewhere in the `Sql` partial class, outside this run's diff). Grew the file from a stub to ~3200 lines / ~90 extension methods:
  - Ranking/navigation: `RowNumber`, `Rank`, `DenseRank`, `PercentRank`, `CumeDist`, `NTile` (`:533-701`); `Lead`/`Lag` (3 overloads each, `:702-877`); `FirstValue`/`LastValue`/`NthValue` (`:879-961`).
  - Aggregates as window functions: `Count`/`LongCount` (`:962-1010`), `Sum`/`Average`/`Min`/`Max` per numeric type (`int`/`long`/`double`/`decimal`/`float`/`short`/`byte`, nullable variants, `:1049-2626`).
  - Statistical/ordered-set: `RatioToReport`, `Median`, `StdDev`/`StdDevPop`/`StdDevSamp`, `Variance`/`VarPop`/`VarSamp`, `CovarPop`/`CovarSamp`/`Corr`, the nine `Regr*` regression functions, `PercentileCont`/`PercentileDisc` (windowed and grouped forms, `:2627-2977`).
  - Hypothetical-set: `Rank`/`DenseRank`/`PercentRank`/`CumeDist` on `IEnumerable<T>` `WITHIN GROUP` (`:3010-3206`).
  - ~25 marker interfaces (`IOPartitionROrderFinal`, `IAggregateFinal`, `ILeadLagFinal`, `IValueFinal`, `INthValueFinal`, `IKeepPart<...>`, etc.) form a compile-time type-state fluent chain for `PartitionBy`/`OrderBy`/`ThenBy`/`RowsBetween`/`RangeBetween`/`GroupsBetween`/`ExcludeCurrentRow`/`ExcludeGroup`/`ExcludeTies`/`Filter`/`Distinct`/`IgnoreNulls`/`RespectNulls`/`FromFirst`/`FromLast`/`KeepFirst`/`KeepLast`/`DefineWindow`/`UseWindow`. These interface methods have **no runtime implementation** -- the lambda passed to each `Sql.Window.*` call is inspected as an expression tree entirely by EXPR-TRANS's `WindowFunctionsMemberTranslator` (owned there, not here), which builds the `SqlExtendedFunction` (with `KeepClause`, `FrameClause`, `NullTreatment`, `FromPosition`) that `BasicSqlBuilder.BuildSqlExtendedFunction` renders.
  - Gating: unsupported functions/clauses throw a descriptive `LinqToDBException` at translation time rather than emitting invalid SQL (see `WindowFunctions.FeatureMatrix.md`).

- **`WindowFunctions.FeatureMatrix.md`** (`Source/LinqToDB/Sql/WindowFunctions.FeatureMatrix.md`) -- **new file, this run (PR #5468)**. Per-provider capability matrix for every `Sql.Window.*` function/clause (ranking, navigation, aggregate/statistical/ordered-set, frame/clause options), plus named per-provider exceptions: Informix `STDDEV_SAMP`/`VAR_SAMP` asymmetry (documented `STDEV`/bare `STDDEV`/`VARIANCE` are population, not sample -- verified empirically against IDS 14), SAP HANA `CORR` without `COVAR_POP`/`COVAR_SAMP`, and a documented **remaining fidelity gap**: ClickHouse `MEDIAN` is intentionally left unsupported because ClickHouse's `median`/`medianExact` aggregates are semantically different (approximate/reservoir-sampling or non-interpolating) from the `PERCENTILE_CONT(0.5)`-equivalent this API models.

- **`Sql.CurrentTimestampUtc`** (`Source/LinqToDB/Sql/Sql.cs:1184`) -- **new `DateTime` property added in PR #5467**. No `[Function]`/`[Property]`/`[ServerSideOnly]` attribute decoration; translation is entirely translator-driven via `DateFunctionsTranslatorBase.TranslateUtcNow` in the EXPR-TRANS layer. Client-side fallback returns `DateTime.UtcNow`. Providers without a `TranslateUtcNow` override silently fall through to client-side evaluation -- see Known issues.

- **`Sql.CurrentTzTimestamp`** (`Source/LinqToDB/Sql/Sql.cs:1193-1198`) -- **new `DateTimeOffset` property added in PR #5467**. Attribute-decorated with per-provider mappings: `[Function(PN.SqlServer, "SYSDATETIMEOFFSET", ServerSideOnly=true)]`, `[Function(PN.PostgreSQL, "now", ServerSideOnly=true)]`, `[Property(PN.Oracle, "SYSTIMESTAMP", ServerSideOnly=true)]`, `[Function(PN.ClickHouse, "now", ServerSideOnly=true)]`, `[Function(PN.Ydb, "CurrentUtcTimestamp", ServerSideOnly=true)]`. Client-side fallback returns `DateTimeOffset.Now`. Providers not listed (e.g. MySQL, SQLite, DB2, Firebird) have no server-side mapping -- queries using `CurrentTzTimestamp` on those providers will evaluate client-side without error.

- **`Sql.Expr<T>`** (`Source/LinqToDB/Sql/Sql.Expressions.cs:559-570`) -- **delta (PR #5526):** `[SqlQueryDependentParams]` removed from the `parameters: object[]` argument of `Sql.Expr<T>(RawSqlString sql, params object[] parameters)`. The `[SqlQueryDependent]` annotation on the `sql` parameter is retained. `[SqlQueryDependentParams]` is now absent from the entire codebase -- searches confirm zero remaining uses across `Source/LinqToDB/`.

## Files (Tier 1 / Tier 2)

**Tier 1** (4 / 4 visited in full):

- `Source/LinqToDB/Internal/SqlProvider/BasicSqlBuilder.cs`
- `Source/LinqToDB/Internal/SqlProvider/BasicSqlOptimizer.cs`
- `Source/LinqToDB/Internal/SqlProvider/ISqlBuilder.cs`
- `Source/LinqToDB/Internal/SqlProvider/ISqlOptimizer.cs`

**Tier 2** (48 candidates: 19 under `Internal/SqlProvider/`, 29 under `Sql/`):

Visited under `Internal/SqlProvider/` (19/19): `BasicSqlBuilder{T}.cs`, `BasicSqlBuilder.Merge.cs`, `SqlExpressionOptimizerVisitor.cs`, `SqlExpressionConvertVisitor.cs`, `JoinsOptimizer.cs`, `JoinsOptimizer.RemoveDuplicates.cs`, `OptimizationContext.cs`, `SqlExpressionFactory.cs`, `SqlOptimizerExtensions.cs`, `SqlProviderFlags.cs`, `ConvertType.cs`, `RowFeature.cs`, `TableIDInfo.cs`, `TableOptionsExtensions.cs`, `HintExtensionBuilder.cs`, `HintWithParameterExtensionBuilder.cs`, `HintWithFormatParametersExtensionBuilder.cs`, `HintWithParametersExtensionBuilder.cs`.

Visited under `Sql/` (29/29): `Sql.cs`, `Sql.ExpressionAttribute.cs`, `Sql.ExtensionAttribute.cs`, `Sql.Expressions.cs`, `Sql.PropertyAttribute.cs`, `Sql.FunctionAttribute.cs`, `Sql.QueryExtensionAttribute.cs`, `Sql.QueryExtensionScope.cs`, `Sql.TableID.cs`, `Sql.TableIDType.cs`, `Sql.IsNullableType.cs`, `Sql.Aggregate.cs`, `Sql.Analytic.cs`, `Sql.GroupBy.cs`, `Sql.Window.cs` (delta read this run -- grew from stub to full ~3200-line implementation, PR #5468), `Sql.Between.cs`, `Sql.Special.cs`, `Sql.Types.cs`, `Sql.Collate.cs`, `Sql.EnumAttribute.cs`, `Sql.TableExpressionAttribute.cs`, `Sql.TableFunctionAttribute.cs`, `Sql.Strings.cs`, `Sql.DateTime.cs`, `Sql.Row.cs`, `Sql.DateOnly.cs`, `Sql.DateTimeOffset.cs`, `Sql.Row.generated.cs` (confirmed generator output, no new public API), `WindowFunctions.FeatureMatrix.md` (new this run, PR #5468 -- capability-matrix doc, not a partial-class source file).

## Subsystems

1. **Statement-level orchestration** -- `BasicSqlBuilder.BuildSql` (`BasicSqlBuilder.cs:179`) dispatches by `Statement.QueryType`. Per-verb builders set `BuildStep` to one of the `Step` enum values (`BasicSqlBuilder.cs:4048`) and call clause-emitting methods in dialect order.
2. **Clause emission** -- one virtual per clause. Sub-tree emission delegates to `BuildExpression(StringBuilder sb, ISqlExpression expr, ...)` (`BasicSqlBuilder.cs:3036`) which dispatches by `ElementType`.
3. **Identifier escaping** -- `Convert(StringBuilder, string, ConvertType)` (`BasicSqlBuilder.cs:606`, default no-op). Provider subclasses override; call site indicates *which kind* of identifier via `ConvertType`.
4. **CTE emission** -- `BuildWithClause` (`BasicSqlBuilder.cs:670`). Reads `IsRecursiveCteKeywordRequired`, `IsCteColumnListSupported`, `SupportsMaterializedCteHint`, `CteFirst`. The `Materialized` annotation pathway lets PostgreSQL 12+, SQLite 3.35+, ClickHouse 26.3+ emit `AS MATERIALIZED` / `AS NOT MATERIALIZED`.
5. **MERGE emission** -- split into `BasicSqlBuilder.Merge.cs` (510 lines). `BuildMergeStatement` (`BasicSqlBuilder.Merge.cs:54`) drives `BuildMergeInto` -> `BuildMergeSource` -> `BuildMergeOn` -> per-operation `BuildMergeOperation` -> `BuildOutputSubclause` -> `BuildMergeTerminator`. Hooks `IsValuesSyntaxSupported`, `IsEmptyValuesSourceSupported`, `FakeTable`, `FakeTableSchema`, `RequiresConstantColumnAliases`, `SupportsColumnAliasesInSource` (and now `SupportsColumnAliasesInScalarSource`, see delta) accommodate dialects without `VALUES (...)` source syntax.
6. **String-concat emission** (delta: PR #5504) -- `BuildSqlConcatExpression` (`BasicSqlBuilder.cs:3896`) lowers `SqlConcatExpression` AST nodes to dialect SQL. Three built-in strategies via `ConcatBuildStyle`: `Plus` (`a + b + c`), `Pipes` (`a || b || c`), `Function` (`CONCAT(a, b, c)`). Provider subclasses set `ConcatStyle` and optionally `ConcatFunctionName`; override `BuildSqlConcatExpression` only for non-standard shapes. Pre-emission operand normalization (cast to string, null-as-empty coalesce wrap) is done by `SqlExpressionConvertVisitor.ConvertConcat` in the convert phase -- the builder receives an already-normalized operand list.
7. **Statement-level optimization** -- `BasicSqlOptimizer.Finalize`. Drives `SqlNullsOrderingLoweringVisitor` (new -- when `!IsNullsOrderingSupported`), then `SelectQueryOptimizerVisitor`, then optionally `JoinsOptimizer`, then per-verb finalization, then per-provider `FinalizeStatement`. NULL/parameter casting across UNION arms uses `RequiresCastingParametersForSetOperations` / `RequiresCastingNullValueForSetOperations`. `SqlRowExpandVisitor` expands `SqlRowExpression` columns before emission.
8. **Two-phase visitor pipeline** -- `OptimizationContext.OptimizeAndConvert<T>` (`OptimizationContext.cs:131`) runs `SqlExpressionOptimizerVisitor.Optimize` (algebraic simplification) followed by `SqlExpressionConvertVisitor.Convert` (dialect-specific translation).
9. **COALESCE chain simplification** (delta: PR #5502) -- `SqlExpressionOptimizerVisitor.VisitSqlCoalesceExpression` (`SqlExpressionOptimizerVisitor.cs:1416`) performs two rewrites in one pass: (1) nested coalesce flattening (inlines `SqlCoalesceExpression` children into the outer list); (2) early-termination on a provably non-nullable mid-chain expression. `SqlExpressionConvertVisitor.RemoveNullValues` (`SqlExpressionConvertVisitor.cs:1247`) strips NULL-literal operands before provider-specific folding, preventing Informix/Access artifacts (issue #5531).
10. **NULLS ordering lowering** (delta -- this run) -- `SqlNullsOrderingLoweringVisitor` (`BasicSqlOptimizer.cs:62`) translates `NULLS FIRST`/`NULLS LAST` ORDER BY clauses into `CASE WHEN <expr> IS NULL THEN 0/1 END` sort keys for providers with `!IsNullsOrderingSupported`. Runs before `OptimizeQueries` so downstream rewrites treat CASE expressions as ordinary derived order expressions. Controlled by `IsNullsOrderingSupported` and `DefaultNullsOrdering` on `SqlProviderFlags`.

10. **Update-path rewrite helpers** -- `BasicCorrectUpdate` and `RemoveUpdateTableIfPossible` handle the gap between `UPDATE` semantics across providers.
11. **Public `Sql.*` extension surface** -- `Source/LinqToDB/Sql/`. Every server-side call lands here as `[Expression]`/`[Function]`/`[Extension]`-decorated method.
12. **Hint/extension builders** -- `HintExtensionBuilder` and three siblings convert a `SqlQueryExtension` AST node back into the textual form the provider wants. Resolves `Sql.SqlID` placeholders through `ISqlBuilder.BuildSqlID`.
13. **DateTime current-time surface** (delta: PR #5467) -- four related properties on `Sql` cover the "current timestamp" use case: `Sql.CurrentTimestamp` (`[ServerSideOnly]`, no client fallback), `Sql.CurrentTimestamp2` (dual-mode, client returns `DateTime.Now`), `Sql.CurrentTimestampUtc` (new, no attribute, translator-driven via `DateFunctionsTranslatorBase.TranslateUtcNow`, client fallback `DateTime.UtcNow`), and `Sql.CurrentTzTimestamp` (new, `DateTimeOffset`-typed, five provider-specific `[Function]`/`[Property]` attributes, client fallback `DateTimeOffset.Now`). `GetDate()` remains the original dual-mode variant. See Known issues for the `CurrentTimestampUtc` attribute-gap debt.
14. **`DateDiff` provider matrix** (delta: PR #5467) -- the `DateDiff` builder dispatch table is now uniform across all three `DateTime`/`DateOnly`/`DateTimeOffset` overloads: all carry entries for `PN.ClickHouse` (`DateDiffBuilderClickHouse`), `PN.Ydb` (`DateDiffBuilderYdb`), and `PN.DuckDB` (routes to `DateDiffBuilderPostgreSql`). `Sql.DateOnly.cs:65-69` and `Sql.DateTimeOffset.cs:72-77` previously lacked these three providers. **Delta (this run):** `DateDiffBuilderYdb` (`Sql.DateTime.cs`) had a correctness bug -- the `endDate - startDate` subtraction was typed `int`, silently truncating the YQL `Interval` result before the divide-by-microseconds-per-unit step; now the subtraction is explicitly typed `Interval`, `CAST`ed to `Int64` (its microsecond count), and *then* divided by the per-unit divisor, matching what YQL actually returns for a datetime subtraction.
15. **Window-function DSL & emission** (delta -- this run, PR #5468) -- `WindowFunctionBuilder` (`Sql.Window.cs`) is the public fluent front-end; `BasicSqlBuilder.BuildSqlExtendedFunction` (`BasicSqlBuilder.cs:3538`) is the emission back-end. Between them sits EXPR-TRANS's `WindowFunctionsMemberTranslator` (not owned here), which turns the fluent-lambda expression tree into a `SqlExtendedFunction` carrying `PartitionBy`/`OrderBy`/`FrameClause`/`Filter`/`WithinGroup`/`KeepClause`/`NullTreatment`/`FromPosition`. The builder renders each of those independently: NULLS treatment placement varies by provider (`WindowNullsPlacement`), frame `EXCLUDE` and boundary direction are new, `KEEP (DENSE_RANK FIRST|LAST ...)` is Oracle-only surface exposed generically. Per-provider support is gated by capability flags on `WindowFunctionsMemberTranslator` subclasses (EXPR-TRANS) and documented exhaustively in `Sql/WindowFunctions.FeatureMatrix.md`; unsupported combinations throw `LinqToDBException` at translation time rather than emitting invalid SQL.
16. **Fluent Upsert predicate support** (delta -- this run, PR #5482) -- `Upsert(...).Update(v => v.When(...))` / `Insert(i => i.When(...))` predicates route through one of three emission paths depending on provider capability flags: native single-statement InsertOrUpdate (gated by `IsInsertOrUpdateWithPredicateSupported`), MERGE lowering with `WHEN [NOT] MATCHED AND <cond>` (gated by `IsUpsertMergeWithPredicateSupported` when `IsUpsertWithMergeLoweringSupported` is also true), or the 3-query `SetIfExistsUpdateElseInsert` orchestration (the only path when the provider's native shape -- e.g. `IF @@ROWCOUNT = 0`-style -- cannot represent an UPDATE-branch predicate at all). `IsUpsertUpdateWhereAfterSet` picks between the two native-MERGE-predicate spellings (`WHEN MATCHED AND <cond> THEN UPDATE` vs `WHEN MATCHED THEN UPDATE SET ... WHERE <cond>`).
17. **DISTINCT ON support** (delta -- this run, PR #5630) -- `SqlProviderFlags.IsDistinctOnSupported` gates a `BuildDistinctModifier`/`BuildDistinctOnExpressions` override on PostgreSQL/DuckDB; `BasicSqlOptimizer.SeparateDistinctFromPagination` treats a `DISTINCT ON` query as a distinct case from plain `DISTINCT` when deciding whether to peel pagination into an outer wrapper query. Falls back to `ROW_NUMBER()`/`OUTER APPLY` emulation (EXPR-TRANS-owned `DistinctBy` translation) when the flag is off.
18. **CTE-as-table generalization** (delta -- this run, PR #5450) -- `SqlCteTable`/`SqlCteTableField` now flow through code previously hard-typed to `SqlTable`/`SqlField` across three files in this area: `BasicSqlBuilder` (expression emission, anchor resolution, alias/physical-name lookup), `BasicSqlOptimizer` (UPDATE-target correction, clone/replace-tree machinery), and `JoinsOptimizer` (duplicate-join detection, unused-left-join removal). This is what lets the new CTE-union eager-loading strategy (owned by LINQ/EXPR-TRANS) target a CTE the same way ordinary `.Update()`/join-optimization code targets a table; `SqlProviderFlags.MaxColumnCount` caps how wide that strategy's synthesized `UNION ALL` projection is allowed to get before it falls back to per-entity preamble queries.

## Interactions

**Inputs.** `BasicSqlBuilder.BuildSql` consumes a finalized `SqlStatement` (see [SQL-AST](../SQL-AST/INDEX.md)) plus an `OptimizationContext`. The statement reaches it after `BasicSqlOptimizer.Finalize` has run.

**Outputs.** A `StringBuilder` of dialect-specific SQL plus `OptimizationContext._actualParameters` (the deduplicated `SqlParameter` list).

**Provider extension points.** Each `<Provider>SqlBuilder` extends `BasicSqlBuilder<TOptions>` and overrides ~5-40 virtuals. Each `<Provider>SqlOptimizer` extends `BasicSqlOptimizer`. For concat, providers override `ConcatStyle` (and optionally `ConcatFunctionName`, `ConcatRequiresExplicitStringCast`). For NULLS ordering, providers set `IsNullsOrderingSupported` and `DefaultNullsOrdering` on `SqlProviderFlags`. For window functions, providers override `GetWindowNullsPlacement`/`WindowFunctionRespectNullsRequired` on the builder and the `Is*Supported` flags on their `WindowFunctionsMemberTranslator` subclass (EXPR-TRANS). For fluent Upsert, providers set `IsInsertOrUpdateWithPredicateSupported`/`IsUpsertWithMergeLoweringSupported`/`IsUpsertMergeWithPredicateSupported`/`IsInsertOrUpdateRequiresAlignedBranches` and optionally override `IsUpsertUpdateWhereAfterSet`.

**`Sql.*` consumption.** `Sql.<method>` calls in user LINQ queries are picked up by [EXPR-TRANS](../EXPR-TRANS/INDEX.md). `Sql.CurrentTimestampUtc` and `Sql.CurrentTzTimestamp` are dispatched through `DateFunctionsTranslatorBase` registration hooks (`DateFunctionsTranslatorBase.cs:63-74`). `Sql.Window.*` fluent calls are dispatched through `WindowFunctionsMemberTranslator` (EXPR-TRANS), which builds the `SqlExtendedFunction` this area's `BasicSqlBuilder` renders.

**Inbound.**
- [LINQ](../LINQ/INDEX.md) -- calls `ISqlOptimizer.Finalize` and `ISqlBuilder.BuildSql` from the query runner.
- [EXPR-TRANS](../EXPR-TRANS/INDEX.md) -- reads `Sql.*` attribute decorations to translate method calls; produces `SqlConcatExpression` via `TranslateBinaryStringConcat`; produces the `SqlExtendedFunction` shape consumed by the window-function emission subsystem (`WindowFunctionsMemberTranslator`).
- All `PROV-*` areas -- every concrete builder/optimizer overrides this area's virtuals.
- [REMOTE-CLIENT](../REMOTE-CLIENT/INDEX.md) -- uses `SqlOptimizerExtensions.PrepareStatementForRemoting`.

**Outbound.**
- [SQL-AST](../SQL-AST/INDEX.md) -- the entire input/output language.
- [MAPPING](../MAPPING/INDEX.md) -- `MappingSchema.GetDbDataType` and `ValueToSqlConverter` are read constantly during emission.
- `Linq.Translation.ISqlExpressionFactory` -- produced by `BasicSqlOptimizer.CreateSqlExpressionFactory`.

## Known issues / debt

Pointers only:

- **`BasicSqlBuilder` is monolithic.** ~5000 lines in one partial type with ~130 virtual hooks (grew from ~4650/~120 with the window-function and Upsert-predicate emission additions this run).
- **`SqlProviderFlags` shape changes are ABI-breaking on the remote channel.**
- **`Sql.EnumAttribute` is undocumented.** XML doc placeholder reads "TODO: write xml doc what it does (I have no idea)".
- **`Sql.Window` was half-implemented as of the prior indexed commit -- resolved this run.** PR #5468 replaced the "Hidden until fully implemented" stub with the full `WindowFunctionBuilder` fluent DSL (~90 extension methods) plus matching `BasicSqlBuilder` emission (NULLS treatment, KEEP, frame EXCLUDE, FROM LAST) and a dedicated capability doc (`WindowFunctions.FeatureMatrix.md`). Remaining gaps are documented there, not here: `MODE() WITHIN GROUP` (non-standard, considered for a future release) and the cosmetic `EXCLUDE NO OTHERS` spelling.
- **Two near-identical date helper files.** `Sql.DateOnly.cs` and `Sql.DateTimeOffset.cs` mirror most of `Sql.DateTime.cs`'s API by data type.
- **`SqlExpressionConvertVisitor` is becoming the catch-all.**
- **`Sql.CurrentTimestampUtc` has no attribute decoration.** Unlike `Sql.CurrentTimestamp` (`[ServerSideOnly]`) and `Sql.CurrentTzTimestamp` (five provider `[Function]`/`[Property]` attributes), `Sql.CurrentTimestampUtc` carries no `[Function]`, `[Property]`, or `[ServerSideOnly]` attribute (`Sql.cs:1184`). Translation depends entirely on `DateFunctionsTranslatorBase.TranslateUtcNow` being overridden by the provider. Providers without that override silently evaluate client-side -- no compile-time or runtime warning. New debt from PR #5467.
- **`Sql.CurrentTzTimestamp` has no mapping for MySQL, SQLite, DB2, Firebird, SAP HANA, Informix, Sybase, Access, SQL CE.** Queries using `Sql.CurrentTzTimestamp` on those providers evaluate client-side without any warning.
- **`ConcatBuildStyle.Plus` is the default `ConcatStyle`** but most modern providers use `Pipes` or `Function`. Providers that do not explicitly override `ConcatStyle` silently emit `+`-style concat. (Deliberate default: the base class cannot assume `||` support.)
- ** ABI additive change.** The two new  fields (, ) are additive but any serialized payload from an older build will deserialize without them (defaults: /).
- ** is  for all providers by default.** Providers that do not set this flag emit CASE emulation sort keys even when ORDER BY direction already matches the natural null placement -- harmless but verbose.
- **`BasicSqlBuilder.BuildInsertOrUpdateQueryAsUpdateInsert` cannot represent an UPDATE-branch predicate at all.** (New this run, PR #5482.) A provider whose native InsertOrUpdate shape is the `IF @@ROWCOUNT = 0`-style emulation throws `LinqToDBException` rather than silently dropping `Upsert(...).Update(v => v.When(...))`'s predicate; callers must go through the 3-query orchestration. Intentional fail-loud, not a bug, but a sharp edge for anyone adding a new such provider.
- **`SqlProviderFlags.IsInsertOrUpdateRequiresAlignedBranches` is SAP-HANA-only today.** Only one provider (SAP HANA) sets it; the flag's contract (fallback to UPDATE-then-INSERT emulation on branch divergence) is exercised by a single case, so a second adopter is the first real test of its generality.

## See also

- [SQL-AST](../SQL-AST/INDEX.md) -- the input/output AST; `SqlConcatExpression` is defined there.
- [EXPR-TRANS](../EXPR-TRANS/INDEX.md) -- the producer of `SqlStatement` instances; also hosts `DateFunctionsTranslatorBase.TranslateUtcNow` which drives `Sql.CurrentTimestampUtc` server-side translation; `TranslateBinaryStringConcat` produces `SqlConcatExpression`; `WindowFunctionsMemberTranslator` (and per-provider subclasses) resolves the `Sql.Window.*` fluent DSL into `SqlExtendedFunction`.
- [LINQ](../LINQ/INDEX.md) -- orchestrates calls to `ISqlOptimizer.Finalize` and `ISqlBuilder.BuildSql`.
- [MAPPING](../MAPPING/INDEX.md) -- `MappingSchema`, `ValueToSqlConverter`, `EntityDescriptor`, `ColumnDescriptor`.
- `PROV-*` -- concrete builder/optimizer subclasses per database.
- [architecture/sql-provider.md](../../architecture/sql-provider.md), [architecture/overview.md](../../architecture/overview.md), [architecture/query-pipeline.md](../../architecture/query-pipeline.md).
- `Source/LinqToDB/Sql/WindowFunctions.FeatureMatrix.md` -- per-provider window-function capability matrix (new this run).

## Pointers

- Build/optimize entry points: `ISqlBuilder.BuildSql` (`Source/LinqToDB/Internal/SqlProvider/ISqlBuilder.cs:15`), `ISqlOptimizer.Finalize` (`Source/LinqToDB/Internal/SqlProvider/ISqlOptimizer.cs:13`).
- Default implementations: `BasicSqlBuilder` (`Source/LinqToDB/Internal/SqlProvider/BasicSqlBuilder.cs:26`), `BasicSqlOptimizer` (`Source/LinqToDB/Internal/SqlProvider/BasicSqlOptimizer.cs:22`).
- Step enum (clause-by-clause emission state): `Source/LinqToDB/Internal/SqlProvider/BasicSqlBuilder.cs:4048`.
- Two-phase visitor pipeline: `OptimizationContext.OptimizeAndConvert*` (`Source/LinqToDB/Internal/SqlProvider/OptimizationContext.cs:122-145`); parameter-name normalization: `NormalizeParameterName` (`OptimizationContext.cs:102`).
- Concat enum + hooks: `ConcatBuildStyle` (`BasicSqlBuilder.cs:3874`), `ConcatStyle` (`BasicSqlBuilder.cs:3888`), `ConcatFunctionName` (`BasicSqlBuilder.cs:3894`), `BuildSqlConcatExpression` (`BasicSqlBuilder.cs:3896`).
- Concat lowering in convert phase: `ConvertConcat` (`SqlExpressionConvertVisitor.cs:1261`), `ConcatRequiresExplicitStringCast` (`SqlExpressionConvertVisitor.cs:1259`).
- NULLS ordering lowering: `SqlNullsOrderingLoweringVisitor` invoked at `BasicSqlOptimizer.cs:62`; flags `IsNullsOrderingSupported` (`SqlProviderFlags.cs:604`), `DefaultNullsOrdering` (`SqlProviderFlags.cs:614`).
- COALESCE chain rewrites: `VisitSqlCoalesceExpression` (`SqlExpressionOptimizerVisitor.cs:1416`), nested-flattening + early-termination in one loop; `RemoveNullValues` (`SqlExpressionConvertVisitor.cs:1247`), NULL-literal stripping before provider fold.
- Public `Sql.*` static partial: `Source/LinqToDB/Sql/Sql.cs:27`.
- Attribute hierarchy root: `Source/LinqToDB/Sql/Sql.ExpressionAttribute.cs:32`.
- `Sql.CurrentTimestampUtc` (no-attribute, translator-driven): `Source/LinqToDB/Sql/Sql.cs:1184`.
- `Sql.CurrentTzTimestamp` (attribute-decorated, 5 providers): `Source/LinqToDB/Sql/Sql.cs:1193-1198`.
- `DateDiff` builder implementations (ClickHouse, Ydb, PostgreSQL/DuckDB): `Source/LinqToDB/Sql/Sql.DateTime.cs:459-538` (Ydb builder's Interval/CAST fix this run).
- `Sql.Expr<T>` (no `[SqlQueryDependentParams]` on `parameters` since PR #5526): `Source/LinqToDB/Sql/Sql.Expressions.cs:564-570`.
- `Sql.NewGuid7()` (UUIDv7, PR #5648): `Source/LinqToDB/Sql/Sql.cs:266`.
- `Sql.AsNullable`/`Sql.ToNullable` (translator-driven, no `[Expression]`, PR #5604): `Source/LinqToDB/Sql/Sql.cs:75,98`.
- Window-function DSL entry: `WindowFunctionBuilder` (`Source/LinqToDB/Sql/Sql.Window.cs:27`); emission back-end: `BasicSqlBuilder.BuildSqlExtendedFunction` (`BasicSqlBuilder.cs:3538`), `WindowNullsPlacement` (`BasicSqlBuilder.cs:3513`); capability doc: `Source/LinqToDB/Sql/WindowFunctions.FeatureMatrix.md`.
- Fluent Upsert predicate flags: `SqlProviderFlags.IsInsertOrUpdateWithPredicateSupported` (`:615`), `IsUpsertWithMergeLoweringSupported` (`:631`), `IsUpsertMergeWithPredicateSupported` (`:648`), `IsInsertOrUpdateRequiresAlignedBranches` (`:665`); builder hook `IsUpsertUpdateWhereAfterSet` (`BasicSqlBuilder.cs:120`).
- DISTINCT ON: `SqlProviderFlags.IsDistinctOnSupported` (`SqlProviderFlags.cs:702`), `BasicSqlBuilder.BuildDistinctModifier`/`BuildDistinctOnExpressions` (`BasicSqlBuilder.cs:800,811`).
- CTE-as-table generalization: `BasicSqlOptimizer.BasicCorrectUpdate` doc comment (`BasicSqlOptimizer.cs:261-264`) explains the `ISqlNamedTable` rationale; `MaxColumnCount` (`SqlProviderFlags.cs:675`).
- Oracle correlated UPDATE ROW fix (#5413): `FoldJoinConditionAndDetach` (`BasicSqlOptimizer.cs:1656`).

<details><summary>Coverage</summary>

- Tier 1 (visited / total): 4 / 4 (unchanged from build run)
- Tier 2 (visited / total): 48 / 48 (100%) -- denominator +1 this run (new file `WindowFunctions.FeatureMatrix.md`, PR #5468)
- Tier 3 (skipped, logged): 0

Read (prior run -- delta, SHA 4a478ff14):
- `Source/LinqToDB/Sql/Sql.cs` (lines 1140-1270): Sql.CurrentTimestampUtc (no attribute), Sql.CurrentTzTimestamp (5-provider attribute matrix)
- `Source/LinqToDB/Sql/Sql.DateOnly.cs` (full): DateDiff now includes ClickHouse/Ydb/DuckDB builders
- `Source/LinqToDB/Sql/Sql.DateTimeOffset.cs` (full): same DateDiff alignment
- `Source/LinqToDB/Sql/Sql.DateTime.cs` (full): DateDiffBuilderClickHouse, DateDiffBuilderYdb implementations confirmed; PR #5517 DateTime.Date truncation fix is in EXPR-TRANS (DateFunctionsTranslatorBase), not this file's public API

Read (this run -- delta, SHA 2e67bafc9):
- `Source/LinqToDB/Internal/SqlProvider/BasicSqlBuilder.cs` (lines 1-200, 3235-3250, 3867-3938): `ConcatBuildStyle` enum, `ConcatStyle`/`ConcatFunctionName` virtual properties, `BuildSqlConcatExpression`/`BuildSqlConcatOperatorChain`/`BuildSqlConcatFunctionCall` -- PR #5504 concat-emission subsystem confirmed at lines 3874-3936; `QueryElementType.SqlConcat` dispatch at line 3243.
- `Source/LinqToDB/Internal/SqlProvider/SqlExpressionConvertVisitor.cs` (lines 1-100, 1152-1390): `VisitSqlConcatExpression`, `ConcatRequiresExplicitStringCast`, `ConvertConcat`, `FlattenNestedConcat`, `IsConcatCoalesceWrap` -- PR #5504 convert-phase lowering confirmed; `VisitSqlCoalesceExpression` confirmed unchanged in this file.
- `Source/LinqToDB/Internal/SqlProvider/SqlExpressionOptimizerVisitor.cs` (lines 1-100, 953-1005, 1300-1365, 1390-1455): PR #5502 -- `VisitSqlCoalesceExpression` early-termination on non-nullable mid-chain expression at line 1426 confirmed.
- `Source/LinqToDB/Internal/SqlProvider/BasicSqlOptimizer.cs` (lines 1-100, 401-468, 514-548): `FinalizeSelect`, `FixSetOperationValues` -- no new code attributable to PR #5502 or #5504 in these sections; #5502 fix is in `SqlExpressionOptimizerVisitor`, not `BasicSqlOptimizer`.
- `Source/LinqToDB/Sql/Sql.Expressions.cs` (full, 574 lines): PR #5526 -- `Sql.Expr<T>(RawSqlString, params object[])` at line 567 has no `[SqlQueryDependentParams]` on `parameters`; `[SqlQueryDependentParams]` is absent from entire file; codebase-wide search confirms zero remaining uses.
- `Source/LinqToDB/Sql/Sql.cs` (lines 1-100): no new public API beyond prior delta run.


Read (this run -- delta, SHA b3340aa9):
-  (lines 1-100, 3850-3970):  doc updated to include Sybase ASE.
-  (lines 1-150):  gains  at line 61-62.
-  (lines 1-100, 1155-1300):  extracted at line 1247; Sybase ASE exception at line 1289.
-  (lines 1-100, 1395-1465): nested COALESCE flattening at lines 1429-1437; early-termination at line 1439.
-  (full): two new  fields Order=69/70; / updated.
-  (full): no changes.
-  (lines 1-100, 1170-1190): no new public API.

Read (this run -- delta, SHA 36ee4f82f):
- `Source/LinqToDB/Internal/SqlProvider/BasicSqlBuilder.Merge.cs` (full, 511 lines): PR #5450 -- `SupportsColumnAliasesInScalarSource` added (defaults to `SupportsColumnAliasesInSource`), splitting the VALUES-source vs scalar/raw-SQL-source column-alias question for CTE-union eager loading.
- `Source/LinqToDB/Internal/SqlProvider/BasicSqlBuilder.cs` (full, 5001 lines, read in two passes): PR #5468 window-function emission (`WindowNullsPlacement` enum `:3513`, `GetWindowNullsPlacement`/`WindowFunctionRespectNullsRequired` `:3527,3536`, `BuildSqlExtendedFunction` extended `:3538`, KEEP clause `:3613-3618`, frame `Exclusion` `:3712-3721`, frame boundary PRECEDING/FOLLOWING direction fix, bare `OVER ()` gate fix); PR #5482 `IsUpsertUpdateWhereAfterSet` `:120` + WHERE-after-SET support in `BuildInsertOrUpdateQueryAsMerge`/`AsOnConflictUpdateOrNothing` + throw-guard in `BuildInsertOrUpdateQueryAsUpdateInsert`; PR #5630 `BuildDistinctModifier`/`BuildDistinctOnExpressions` `:800,811`; PR #5450 `SqlCteTableField` build case `:3159`, `BuildAnchor`/`FindTable`/`GetTableAlias`/`GetPhysicalTableName` generalized to `ISqlNamedTable`/`SqlFieldBase`; `BuildEmptyValuesFrom` hook `:2109`.
- `Source/LinqToDB/Internal/SqlProvider/BasicSqlOptimizer.cs` (full, 2597 lines, read in two passes): PR #5450 `ISqlNamedTable` generalization of the UPDATE-correction path (`FindTableSource`, `IsCompatibleForUpdate`, `ApplyUpdateTableComparison`, `BasicCorrectUpdate`, `RemoveUpdateTableIfPossible`, `CloneQuery`, `CloneTable`, `CorrectReplaceTree`); issue #5413 / PR #5584 `FoldJoinConditionAndDetach` column-rebase fix `:1656` and `FlattenRowConstructors` per-column subquery clone fix; PR #5630 `SeparateDistinctFromPagination` excludes `IsDistinctOn` `:2343-2348`.
- `Source/LinqToDB/Internal/SqlProvider/JoinsOptimizer.RemoveDuplicates.cs` (full, 649 lines): PR #5450 -- `IsSimilarFields` gains an `SqlCteTableField` comparison branch (`:599-605`) so CTE fields participate in duplicate-join detection.
- `Source/LinqToDB/Internal/SqlProvider/JoinsOptimizer.cs` (full, 417 lines): PR #5450 -- `UndoNestedJoins` signature change (`:159`, now takes `SqlQueryColumnNestingCorrector`/`SqlQueryColumnOptimizerVisitor`/`MappingSchema`, returns `IQueryElement`, re-runs column optimization); `GetFieldSourceID`/`GetUnderlyingFieldOrColumn`/`CanRemoveLeftJoin` gain `SqlCteTableField` cases.
- `Source/LinqToDB/Internal/SqlProvider/OptimizationContext.cs` (full, ~140 lines): `AddParameter`'s normalization extracted into public `NormalizeParameterName(string?)` `:102`.
- `Source/LinqToDB/Internal/SqlProvider/SqlExpressionConvertVisitor.cs` (diff hunk only, ~2105 lines total): PR #5450 -- `WrapBooleanExpression`'s field-wrap match extended to `QueryElementType.SqlCteTableField` (one-line change, `:1755`).
- `Source/LinqToDB/Internal/SqlProvider/SqlProviderFlags.cs` (full, diff + surrounding context): six new `[DataMember]` fields confirmed -- `IsInsertOrUpdateWithPredicateSupported` (Order=71, `:615`), `IsUpsertWithMergeLoweringSupported` (Order=72, `:631`), `IsUpsertMergeWithPredicateSupported` (Order=73, `:648`), `IsInsertOrUpdateRequiresAlignedBranches` (Order=74, `:665`), `MaxColumnCount` (Order=76, `:675`), `IsDistinctOnSupported` (Order=75, `:702`); all folded into `GetHashCode`/`Equals`.
- `Source/LinqToDB/Sql/Sql.DateTime.cs` (diff hunk only, full file previously read): `DateDiffBuilderYdb` correctness fix -- subtraction now typed `Interval` and `CAST`ed to `Int64` before the per-unit divide (was `int`-typed, silently truncating).
- `Source/LinqToDB/Sql/Sql.ExpressionAttribute.cs` (diff hunk only): doc-comment punctuation fix (`OVER()` -> `OVER ()`), no behavior change.
- `Source/LinqToDB/Sql/Sql.Expressions.cs` (diff hunk only, full file previously read): table-name builder reads `TableOptions` via `(sqlTable as SqlTable)?.TableOptions ?? TableOptions.NotSet` instead of assuming `SqlTable`, avoiding a cast failure when the table-like source is a CTE (PR #5450).
- `Source/LinqToDB/Sql/Sql.Window.cs` (full, 3207 lines -- new implementation, was a stub): PR #5468 -- confirmed `WindowFunctionBuilder` static class (`:27`) with ~90 extension methods and ~25 fluent marker interfaces; confirmed no runtime method bodies for the interface-declared fluent steps (`PartitionBy`/`OrderBy`/`RowsBetween`/etc. are interface members with no implementing class in this file -- resolved by EXPR-TRANS's `WindowFunctionsMemberTranslator` via expression-tree inspection).
- `Source/LinqToDB/Sql/WindowFunctions.FeatureMatrix.md` (full, 164 lines -- new file): capability matrix confirmed; ClickHouse `MEDIAN` gap and Informix/SAP-HANA statistical-function asymmetries documented with citations to specific flags.
- `Source/LinqToDB/Sql/Sql.cs` (diff hunk only, full file previously read for the Sql.CurrentTimestampUtc/CurrentTzTimestamp region): `Sql.NewGuid7()` `:266` (PR #5648) and `Sql.AsNullable`/`Sql.ToNullable` losing `[Expression]` `:75,98` (PR #5604) confirmed; also confirmed several new `[Expression(PN.Ydb, ...)]` decorations on `Cot`/`Degrees` overloads (PR #5564, YDB math-function coverage) -- not otherwise called out above as they don't change this area's structural surface.
</details>
