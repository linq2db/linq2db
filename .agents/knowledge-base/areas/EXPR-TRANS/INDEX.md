---
area: EXPR-TRANS
kind: area-index
sources: [code]
confidence: high
last_verified: 2026-07-05
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
coverage_tier_1: 68/68
coverage_tier_2: 86/86
---

# EXPR-TRANS -- LINQ expression -> SQL AST translation

The pipeline that consumes a `System.Linq.Expressions.Expression` (a LINQ method-call tree on `IQueryable<T>` / `ITable<T>`) and produces an `IBuildContext` graph whose `SelectQuery` / `SqlStatement` is then handed to [`SQL-PROVIDER`](../SQL-PROVIDER/INDEX.md) for optimization and emission. Every translated LINQ operator (`Where`, `Select`, `OrderBy`, `Join`, `GroupBy`, `Insert`, `Update`, `Delete`, `Merge`, `Concat`, `Distinct`, `Take`/`Skip`, `Cast`, `OfType`, `AsCte`, `FromSql`, `AsQueryable`, `DistinctBy`, `MinBy`/`MaxBy`, `ExceptBy`/`UnionBy`/`IntersectBy`, ...) has a dedicated `*Builder` class; the central `ExpressionBuilder` + a Roslyn-generated dispatcher pick the right one per node. This delta batch (16 merged PRs) adds a fluent `Upsert`/entity-`Insert`/entity-`Update` builder API, a three-strategy eager-loading dispatcher (`Default`/`KeyedQuery`/`CteUnion`), named (multiple) query filters, TPH inheritance fixes, and a rewritten `Sql.Window` translation surface -- see Subsystems 22-35.

## Key types

- ``ExpressionBuilder`` (`Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.cs:26`) -- the per-query orchestrator. Holds the `Query`, `MappingSchema`, `DataOptions`, `ParametersContext`, `ExpressionTreeOptimizationContext`, the `_buildVisitor`, the `IMemberTranslator`, and pools for `SelectQuery` / `ParentInfo` / `PlaceholderCollectVisitor`. Public entry: `Build<T>(ref IQueryExpressions)` (line 124), called by [`LINQ`](../LINQ/INDEX.md)'s query setup. Sequence dispatch entry: `TryFindBuilder` -> `FindBuilderImpl` (line 30, 38) -- `FindBuilderImpl` is a `partial` method whose body is generated.
- ``ISequenceBuilder`` -- the strategy interface every builder implements. Two methods: `BuildSequence` (produce `BuildSequenceResult`) and `IsSequence` (predicate).
- ``MethodCallBuilder`` -- abstract base for the common case "this LINQ node is a `MethodCallExpression`"; subclasses override `BuildMethodCall(builder, methodCall, buildInfo)`. Default `IsSequence` recurses into `Arguments[0]` when `IQueryable`.
- ``IBuildContext`` -- the per-stage context produced by a builder. Carries `Builder`, `MappingSchema`, `Expression`, `SelectQuery`, `Parent`, `TranslationModifier`, `ElementType`. Key method `MakeExpression(path, ProjectFlags)` is the projection-resolution hook used during materialization-mapper construction. This delta adds `SetElementSelection<T>(Query<T>)` (default no-op) -- configures `Query<T>.GetElement`/`GetElementAsync` cardinality delegates independently of `SetRunQuery`, needed because the eager-load buffer / CteUnion strategies install their own `GetResultEnumerable` and must still wire First/Single cardinality on top of it (`FirstSingleBuilder.SetRunQuery` now calls `SetElementSelection` as a separate step).
- ``BuildContextBase`` / ``SequenceContextBase`` -- abstract base classes; `SequenceContextBase` adds a `Sequences[]` array and `Body` lambda, used by builders that wrap a child sequence.
- ``BuildInfo`` -- the per-call invocation record. Mutable bag carrying `Parent`, `Expression`, `SelectQuery`, `CreateSubQuery`, `IsAssociation`, `JoinType`, `IgnoreOrderBy`, `IsAggregation`, `SourceCardinality`. Cloned + retargeted as recursion descends through nested method calls.
- ``BuildSequenceResult`` -- return value of `BuildSequence`. Encodes three states via factories: `FromContext(IBuildContext)`, `Error(Expression)`, `NotSupported()`.
- ``BuildsAnyAttribute`` / ``BuildsExpressionAttribute`` / ``BuildsMethodCallAttribute`` -- attribute markers consumed by the source generator.
- ``ProjectFlags`` / ``BuildPurpose`` / ``BuildFlags`` -- bitmasks that drive the `MakeExpression` / `BuildExpression` path.
- ``ContextRefExpression`` (in [`EXPR`](../EXPR/INDEX.md), `LinqToDB.Internal.Expressions`) -- the expression-tree node that wraps an `IBuildContext` once a sequence root has been resolved.
- ``TranslationModifier`` -- sealed immutable class propagated through every `IBuildContext`. Carries `InlineParameters`, `IgnoreQueryFilterScopes` (`FilterIgnoreScope[]?` -- renamed/generalized this delta from the old flat `IgnoreQueryFilters` `Type[]?`), and (new) `EagerLoadingStrategy?`. `WithIgnoreQueryFilterScope` appends a `FilterIgnoreScope` (dedup by `Equals`); `WithIgnoreQueryFilters(Type[])` is now a back-compat shim that wraps its argument in a wildcard-key scope. `IsFilterDisabled(Type)` (whole-entity) and the new `IsFilterDisabled(Type, string filterKey)` (per-named-filter) both scan the scope array.
- ``ParametersContext`` -- manages `SqlParameter` allocation and the `ExpressionCacheManager` for dynamic expression accessors. This delta adds `ReplaceDynamicColumnGetters` (`ParametersContext.cs:123`), invoked from `BuildParameter`: rewrites `[DynamicColumnsStore]` member accesses in a parameter-value expression to the column's store-aware getter, because a plain dynamic-column member access compiles to `DynamicColumnInfo`'s throwing dummy getter -- needed by the entity Insert/Update/Upsert auto-derive path, which reads column values directly off an in-memory instance.
- ``IAsQueryableBuilder<T>`` (`Source/LinqToDB/Linq/IAsQueryableBuilder.cs`) -- marker-only public interface (PR #5495). Methods (`Parameterize()`, `Inline()`) are never called at runtime -- captured as expression nodes and interpreted at query-build time by `EnumerableBuilder.TryParseConfigure`.
- ``IAsQueryableExceptBuilder<T>`` (`Source/LinqToDB/Linq/IAsQueryableExceptBuilder.cs`) -- second stage of the same configuration chain.
- ``EnumerableParameterizationConfig`` (`Source/LinqToDB/Internal/Linq/Builder/EnumerableParameterizationConfig.cs`) -- sealed internal class produced by `EnumerableBuilder.BuildConfigured`. Carries `DefaultForceParameter`, `Parameter`, `Excepted`. `ShouldForceParameter(Expression)` decides `SqlParameter` vs `SqlValue` per VALUES column.
- ``SqlAggregateLifterExpression`` (`Source/LinqToDB/Internal/Expressions/SqlAggregateLifterExpression.cs`) -- wraps a `SqlPlaceholderExpression` with `MaterializationCheck`/`SqlRewriter` delegates for non-nullable aggregate COALESCE/null-check handling.
- ``BuildAggregationFunctionResult`` (`Source/LinqToDB/Linq/Translation/BuildAggregationFunctionResult.cs`) -- record returned from every `functionFactory` delegate. `Skipped()` sentinel bails an Expression-mode translation out to cascade fallback.
- ``AggregateFunctionBuilder`` (`Source/LinqToDB/Linq/Translation/AggregateFunctionBuilder.cs`) -- public fluent builder for aggregate/window-function translation; `ModeConfig.IsServerSideOnly`/`ItemTransform`/`ValueTransform`.
- ``CteAnnotationsContainer`` (`Source/LinqToDB/Internal/Linq/Builder/CteAnnotationsContainer.cs`) -- immutable `ICteBuilder` snapshot used as a query-cache key; ordinal-sorted `BuildCacheKey`.
- ``IUpsertSpec<TTarget>`` / ``IUpsertInsertSpec<TTarget>`` / ``IUpsertUpdateSpec<TTarget>`` (`Source/LinqToDB/Linq/IUpsertSpec.cs`, `IUpsertInsertSpec.cs`, `IUpsertUpdateSpec.cs`) -- **new public API (PR #5482)**. Fluent configuration interfaces for `table.Upsert(item, cfg => cfg.Match(...).Set(...).Insert(i => ...).Update(v => ...))`. `IUpsertSpec.Match((t,s) => ...)` sets the row-match key (defaults to PK); `Set`/`Ignore` on the root apply to both branches; `Insert(i => i.When(...).DoNothing())` / `Update(v => v.When(...).DoNothing())` configure per-branch predicates and skips. All chain calls are captured as expression-tree method calls and parsed by `UpsertBuilder`/`EntityBuilderParser` -- never invoked at runtime (same contract as `IAsQueryableBuilder<T>`).
- ``IEntityInsertSpec<TTarget>`` / ``IEntityUpdateSpec<TTarget>`` (`Source/LinqToDB/Linq/IEntityInsertSpec.cs`, `IEntityUpdateSpec.cs`) -- **new public API (PR #5482)**. Standalone single-entity `table.Insert(item, cfg => cfg.Set(...).Ignore(...))` / `table.Update(item, cfg => ...)` configuration surfaces; F-bounded (`IEntityInsertSpec<TTarget, TBuilder> where TBuilder : IEntityInsertSpec<TTarget, TBuilder>`) so chain methods return the concrete leaf builder type, and `IUpsertInsertSpec<T>`/`IUpsertUpdateSpec<T>` reuse the same base to add `When`/`DoNothing`.
- ``UpsertBuilder`` (`Source/LinqToDB/Internal/Linq/Builder/UpsertBuilder.cs`) -- `[BuildsMethodCall(nameof(LinqExtensions.Upsert), nameof(LinqExtensions.UpsertAsync))]`. Parses the configure-lambda into a private `UpsertConfig`, then picks one of two lowering paths: native `SqlInsertOrUpdateStatement` (`UpsertContext`; single-item + PK-match + no `SkipInsert`/`InsertWhen`) or a synthesized `Merge()...Using()...On()...InsertWhenNotMatchedAnd()...UpdateWhenMatchedAnd()...Merge()` expression tree re-entering `TryBuildSequence` and lowered through the existing `MergeBuilder` pipeline (bulk source, `SkipInsert`, `InsertWhen`, or non-PK `.Match`). `HasDivergentInsertOrUpdateBranches` / `MatchKeysDivergeFromPrimaryKey` (both `internal static`, `UpsertBuilder.cs:664`, `:695`) are shared with `InsertOrUpdateBuilder` to detect when the native single-statement form (e.g. SAP HANA `UPSERT ... WITH PRIMARY KEY`) cannot honor the requested shape and must fall back to UPDATE->INSERT emulation.
- ``EntityInsertBuilder`` / ``EntityUpdateBuilder`` (`Source/LinqToDB/Internal/Linq/Builder/EntityInsertBuilder.cs`, `EntityUpdateBuilder.cs`) -- **new (PR #5482)**. Translate the 3-arg `Insert<T>(ITable<T>, T, configure)` / `Update<T>(ITable<T>, T, configure)` entity-builder overloads by synthesizing the existing `Insert<T>(ITable<T>, Expression<Func<T>>)` / `Update<T>(IQueryable<T>, predicate, setter)` call shapes and re-entering `TryBuildSequence`. `InsertBuilder.CanBuildMethod` / `UpdateBuilder.CanBuildMethod` explicitly exclude this shape (`!EntityInsertBuilder.IsEntityInsertShape(call)` / `!EntityUpdateBuilder.IsEntityUpdateShape(call)`) so both builders can share the `Insert`/`Update` method names without colliding.
- ``EntitySetterBuilder`` (`Source/LinqToDB/Internal/Linq/Builder/EntitySetterBuilder.cs`) -- shared static helper building `s => new T { ... }` / `(t, s) => new T { ... }` setter lambdas from an `EntityDescriptor` columns list plus `Set`/`Ignore` overlays, emitted as `SqlGenericConstructorExpression` rather than `MemberInit(New(T), ...)` (so a non-default-constructible entity type still works). Shared by `EntityInsertBuilder`, `EntityUpdateBuilder`, and `UpsertBuilder` MERGE-lowering path. `BuildBindings` groups dotted nested-column members into `MemberMemberBinding`. `FindOverride`/`IsIgnored` do structural-equality lookup via `ExpressionEqualityComparer` against a canonicalised field-selector key (`EntityBuilderParser.Canonicalise`, last-match-wins).
- ``EntityBuilderConfig`` / ``EntityBuilderParser`` (`Source/LinqToDB/Internal/Linq/Builder/EntityBuilderConfig.cs`, `EntityBuilderParser.cs`) -- shared parser for the `Expression<Func<I*Spec<T>, I*Spec<T>>>` configure-chain common to standalone Insert/Update and Upsert branches; walks the method-call chain outermost-first then processes deepest-first so later `.Set` calls win.
- ``FilterIgnoreScope`` (`Source/LinqToDB/Internal/Linq/Builder/FilterIgnoreScope.cs`) -- **new (PR #5525)**. Immutable two-dimensional (filter-key `string[]?` x entity-type `Type[]?`) match predicate pushed onto `TranslationModifier.IgnoreQueryFilterScopes` by `IgnoreFilters`/`IgnoreFiltersByKey`/`PushDisabledQueryFilters`. `Keys == null` is the "any key" wildcard (legacy type-only `IgnoreFilters(Type[])` behaviour); `Types == null`/empty means "any type". Arrays are sorted+deduped at construction (`FilterIgnoreScope.cs:18`) so scopes built from the same members in different order compare equal -- load-bearing for the `TranslationModifier` equality-keyed query cache.
- ``EagerLoadingStrategy`` (`Source/LinqToDB/EagerLoadingStrategy.cs`, public, root `LinqToDB` namespace, outside this area globs) -- **new enum (PR #5450)**: `Default` (existing SELECT-DISTINCT-plus-SelectMany preamble), `KeyedQuery` (buffer main results, extract distinct parent keys client-side, single batched child query via a `VALUES` key table), `CteUnion` (batch every `WithUnionLoadStrategy` child at the same level into one `UNION ALL` carrier query; remaps to `KeyedQuery` when CTEs/window functions are not supported). Resolved per-query by `ExpressionBuilder.ResolveStrategy` (`ExpressionBuilder.EagerLoad.cs:536`).
- ``WithLoadStrategyBuilder`` (`Source/LinqToDB/Internal/Linq/Builder/WithLoadStrategyBuilder.cs`) -- **new (PR #5450)**. `[BuildsMethodCall(WithUnionLoadStrategy, WithSeparateLoadStrategy, WithKeyedLoadStrategy)]`; pushes the corresponding `EagerLoadingStrategy` onto `TranslationModifier` for the marked subquery via `PushTranslationModifier`, first-marker-wins (an inner marker is a no-op if an outer one already set a strategy).
- ``EagerLoadState`` / ``EagerLoadFallbackReason`` (`ExpressionBuilder.EagerLoadState.cs`, `EagerLoadFallbackReason.cs`) -- **new (PR #5450)**. `EagerLoadState` is per-strategy-attempt scratch state (committed to `ExpressionBuilder._eagerLoadState` only on success, discarded on failure -- no rollback plumbing needed since nothing outside `CompleteEagerLoadingExpressions` observes it before commit). `EagerLoadFallbackReason` is a diagnostic-only enum (`NoEagerLoads`, `NestedBatchNotSupported`, `NoBranches`, `KeyTypeMismatch`, `MaxColumnCountExceeded`, `ComplexParentReference`, `BatchCacheMiss`, `NestedCorrelationNotFound`) recording why an attempt fell back; surfaced via `ExpressionBuilder.LastEagerLoadFallbackChain` for tests/traces, never branched on by the dispatcher.
- ``ImplicitEagerLoadGuardVisitor`` (`Source/LinqToDB/Internal/Linq/Builder/ImplicitEagerLoadGuardVisitor.cs`) -- **new (PR #5450)**. Read-only pass over the top-level projection when `DataOptions.LinqOptions.ImplicitCollectionLoading == ImplicitCollectionLoading.Throw`; throws unless every `SqlEagerLoadExpression` sits inside a `MarkerExpression(MarkerType.ExplicitEagerLoad)` subtree (set by `EntityConstructorBase.BuildGenericFromMembers`, `EntityConstructorBase.cs:216`, around genuine `LoadWith`/`ThenLoad` collection members). Guarded once per top-level query via `ExpressionBuilder._inEagerLoadProcessing` (`ExpressionBuilder.QueryBuilder.cs:33`) so recursive preamble sub-builds inside `CompleteEagerLoadingExpressions` do not re-trip it.
- ``TranslationProviderFlags`` (`Source/LinqToDB/Linq/Translation/TranslationProviderFlags.cs`) -- **new public type (PR #5468)**. Read-only projection of the translation-relevant subset of [SQL-PROVIDER] `SqlProviderFlags` (`DefaultNullsOrdering`, `IsNullsOrderingSupported`) exposed to member translators via `ITranslationContext.ProviderFlags` / `ExpressionBuilder.TranslationProviderFlags` (`ExpressionBuilder.cs:246`, cached per `ExpressionBuilder` instance).
- ``ITableContext.NamedTable`` (was `SqlTable`) -- widened from a concrete `SqlTable` to `ISqlNamedTable` ([SQL-AST]) so `TableBuilder.TableContext`, `TableBuilder.RawSqlContext`, and `CteTableContext` share one property returning the provider-agnostic named-table interface (a `SqlTable` or `SqlCteTable`). Enables `CteContext`/`CteTableContext` to carry `SqlCteField`/`SqlCteTableField` ([SQL-AST]) columns through the same code paths `TableContext` uses for plain `SqlField`. `TableBuilder.TableContext` still exposes the concrete `SqlTable` property alongside `NamedTable => SqlTable`.

## Files (Tier 1 / Tier 2)

### Tier 1 -- pinned (68 files)

`ExpressionBuilder.cs` plus every `*Builder.cs` at the folder root. `AggregateExecuteBuilder.cs` and `UpdateBuilder.cs` are included here.

Net-8-only builders (conditional `#if NET8_0_OR_GREATER`): `DistinctByBuilder.cs`, `MinByMaxByBuilder.cs`, `SetOperationByBuilder.cs`.

New Tier-1 files this delta (5, all root `*Builder.cs`): `EntityInsertBuilder.cs`, `EntitySetterBuilder.cs`, `EntityUpdateBuilder.cs`, `UpsertBuilder.cs` (all PR #5482), `WithLoadStrategyBuilder.cs` (PR #5450).

### Tier 2 -- implementation / contexts / helpers (86 files)

Examples: `BuildInfo.cs`, `BuildSequenceResult.cs`, `BuildContextBase.cs`, `SequenceContextBase.cs`, `IBuildContext.cs`, `ISequenceBuilder.cs`, `Attributes.cs`, `BuildFlags.cs`, `BuildPurpose.cs`, `ProjectFlags.cs`, `ProjectFlagExtensions.cs`, `ProjectionPathHelper.cs`, `SubQueryContext.cs`, `SelectContext.cs`, `AnchorContext.cs`, `AsSubqueryContext.cs`, `EagerContext.cs`, `EagerLoading.cs`, `ScopeContext.cs`, `PassThroughContext.cs`, `SingleExpressionContext.cs`, `EnumerableContext.cs`, `EnumerableContextDynamic.cs`, `CteContext.cs`, `CteTableContext.cs`, `CteAnnotationsContainer.cs`, `CteBuilderImpl.cs`, `TableLikeQueryContext.cs`, `TableLikeHelpers.cs`, `TableBuilder.TableContext.cs`, `TableBuilder.RawSqlContext.cs`, `TableBuilder.CteTableContext.cs`, `MergeBuilder.*.cs` partials (12), `MergeProjectionHelper.cs`, `AssociationHelper.cs`, `LoadWithEntity.cs`, `LoadWithMember.cs`, `IAnnotatableBuilderInternal.cs`, `IBuildProxy.cs`, `BuildProxyBase{TOwner}.cs`, `ILoadWithContext.cs`, `ITableContext.cs`, `EntityConstructorBase.cs`, `RecordReaderBuilder.cs` helpers, `SequenceHelper.cs`, `EvaluationHelper.cs`, `LambdaResolveVisitor.cs`, `ExpressionBuildVisitor.cs`, `BinaryExpressionAggregatorVisitor.cs`, `ExpressionTreeOptimizerVisitor.cs`, `ExpressionTreeOptimizationContext.cs`, `ExpressionTestGenerator.cs`, `ParametersContext.cs`, `TranslationModifier.cs`, `BuildContextDebuggingHelper.cs`, plus the two files under `Visitors/` (`ExposeExpressionVisitor.cs`, `CanBeEvaluatedOnClientCheckVisitorBase.cs`), plus `ExpressionBuilder.*.cs` partials (`EagerLoad.cs`, `Associations.cs`, `SqlBuilder.cs`, `QueryBuilder.cs`, `Aggregation.cs`, and the 4 new ones below).

New files added by PR #5495 (Tier 2): `EnumerableParameterizationConfig.cs`; `IAsQueryableBuilder.cs` and `IAsQueryableExceptBuilder.cs` live in `Source/LinqToDB/Linq/` (public surface, Tier 2 for this area).

New Tier-2 files this delta (15): `EagerLoadFallbackReason.cs`, `EntityBuilderConfig.cs`, `EntityBuilderParser.cs`, `FilterIgnoreScope.cs`, `ImplicitEagerLoadGuardVisitor.cs`, `ExpressionBuilder.EagerLoadDefault.cs`, `ExpressionBuilder.EagerLoadKeyedQuery.cs`, `ExpressionBuilder.EagerLoadState.cs`, `ExpressionBuilder.EagerLoadUnion.cs` (all PR #5450); `Source/LinqToDB/Linq/IEntityInsertSpec.cs`, `IEntityUpdateSpec.cs`, `IUpsertInsertSpec.cs`, `IUpsertSpec.cs`, `IUpsertUpdateSpec.cs` (all PR #5482); `Source/LinqToDB/Linq/Translation/TranslationProviderFlags.cs` (PR #5468).

Public translation-layer files (outside area globs, integrated here for completeness -- see UNCLASSIFIED-FILE blocks below): `Source/LinqToDB/Linq/Translation/AggregateFunctionBuilder.cs`, `Source/LinqToDB/Linq/Translation/BuildAggregationFunctionResult.cs`, `Source/LinqToDB/Linq/Translation/WindowFunctionsMemberTranslator.cs` (substantially rewritten this delta, PR #5468 -- see Subsystem 26), `Source/LinqToDB/Linq/Expressions.cs`.

## Subsystems

1. **Dispatcher (source-generated).** `ExpressionBuilder.FindBuilderImpl` is generated by `BuildersGenerator` (`Source/CodeGenerators/BuildersGenerator.cs:14`) from `[BuildsExpression]`, `[BuildsMethodCall]`, `[BuildsAny]` markers on each `*Builder` class. The generator emits a single `switch (expr.NodeType)` over `ExpressionType`, with a nested `switch (call.Method.Name)` for the `Call` case, and finally falls through to the `[BuildsAny]` set. Each candidate calls a static `CanBuild` / `CanBuildMethod` predicate; the first that returns `true` wins, and the dispatcher returns the singleton instance from `Builder<T>.Instance`. Adding a new operator = drop a `[BuildsMethodCall("Foo")]`-decorated `FooBuilder.cs`; the generator picks it up next build.

2. **`BuildSequence` recursion.** `ExpressionBuilder.TryBuildSequence(BuildInfo)` is the recursive workhorse: it `ExpandToRoot`s the expression (resolves macros / associations), runs `TryFindBuilder`, asks the chosen builder for a context, registers the originating expression on the context (`RegisterSequenceExpression`), and returns. Most builders themselves call back into `builder.TryBuildSequence(new BuildInfo(buildInfo, methodCall.Arguments[0]))` to translate the source sequence.

3. **Context graph.** Each builder produces an `IBuildContext`. Composite operators (`Where`, `Select`, `OrderBy`, joins) wrap the child context -- typically in a `SubQueryContext` when the parent already has `Distinct` / `Take` / `Skip` / projection. Joins create `outer`/`inner` `SubQueryContext` pairs.

   Context specializations include `SubQueryContext`, `AsSubqueryContext`, `SelectContext`, `ScopeContext`, `PassThroughContext`, `SingleExpressionContext`, `AnchorContext`, `EagerContext`, `EnumerableContext`, `EnumerableContextDynamic`, `CteContext`, `CteTableContext`, `TableBuilder.TableContext`, `TableBuilder.RawSqlContext`, `TableBuilder.CteTableContext`.

   - `ScopeContext` -- carries a `Context` (inner) and an `UpTo` (parent boundary). `Clone(CloningContext)` delegates to `context.CloneContext` for both fields. `MakeExpression` re-routes SQL-mode requests through `Builder.BuildSqlExpression(UpTo, ...)` after building on the inner context.
   - `EnumerableContext` -- context backed by `SqlValuesTable`. Maps LINQ-side member paths to `SqlField` entries in the `VALUES(...)` table. As of PR #5495 the constructor accepts an optional `EnumerableParameterizationConfig? parameterization` parameter; when non-null, `BuildValueGetter` calls `_parameterization.ShouldForceParameter(me)` to choose between emitting `new SqlParameter(...)` and `new SqlValue(...)` per column -- columns whose mapping returns `DataParameter` are always parameters regardless of this config (`EnumerableContext.cs:233-235`).

4. **`SelectQuery` plumbing.** Every builder mutates the running `SelectQuery` (from `BuildInfo.SelectQuery`) -- adding `Where`/`Having` clauses, columns, `OrderBy`, joins. The `SelectQuery` instance is allocated from `ExpressionBuilder.QueryPool` at the entry point and threaded through `BuildInfo`. The final `SqlStatement` is constructed by terminal builders (`InsertBuilder`, `DeleteBuilder`, `UpdateBuilder`, `MergeBuilder`, etc.).

5. **Projection / materialization.** After `BuildSequence` returns the root context, `ExpressionBuilder.BuildQuery<T>` calls `_buildVisitor.BuildExpression(sequence, new ContextRefExpression(typeof(T), sequence), buildPurpose: BuildPurpose.Expression)` to produce the materialization mapper. This walks the context graph via `IBuildContext.MakeExpression(path, ProjectFlags)`.

   `ExpressionBuilder.Project` (in `ExpressionBuilder.SqlBuilder.cs`) handles `Convert(body)` wrapping: when `path` is a `Convert` / `ConvertChecked` node whose operand is a `ContextRefExpression`, `Project` re-types the `ContextRefExpression` to the conversion target type and recurses -- this is the path that resolves interface-member accesses on concrete types during projection (`ExpressionBuilder.SqlBuilder.cs:1808-1815`).

6. **Method-chain extension hook (`MethodChainBuilder`).** Aggregate / window-function calls registered via `Sql.ExtensionAttribute` are dispatched to `MethodChainBuilder` (`[BuildsExpression(ExpressionType.Call)]`) -- not a per-method `*Builder`.

7. **Finalization + handoff to SQL-PROVIDER.** `ExpressionBuilder.BuildQuery<T>` calls `query.SqlOptimizer.Finalize(...)` per query info, then `SqlProviderHelper.IsValidQuery(...)`; failure here surfaces as a `SqlErrorExpression`. This delta guards the finalize-and-validate loop with `if (!query.IsFinalized)` (`ExpressionBuilder.cs:271`) so a query already finalized by an eager-load strategy (CteUnion single-query mode setting `QueryFinalizedRequested`) is not re-finalized.

8. **MergeBuilder partials.** `MergeBuilder` is a `partial class` spread across 10+ files. The top-level `MergeBuilder.cs` (Tier 1) handles execution dispatch and OUTPUT clause handling. Per-operation partials: `Merge`, `MergeInto`, `Using`, `UsingTarget`, `On`, `InsertWhenNotMatched`, `UpdateWhenMatched`, `UpdateWhenMatchedThenDelete`, `DeleteWhenMatched`, `DeleteWhenNotMatchedBySource`, `UpdateWhenNotMatchedBySource`, `MergeContext`, `MergeProjectionHelper`, `TableLikeQueryContext`, `TableLikeHelpers`. `MergeBuilder.ReplaceSourceInQuery` / `GetTargetTable` are widened to `ISqlTableSource` this delta so a `CteTableContext` target (a `SqlCteTable`) can be replaced/read the same way as a plain `SqlTable` target -- needed because `UpsertBuilder.BuildAsMerge` can synthesize a Merge chain over any table context, not just `TableBuilder.TableContext`.

9. **CTE subsystem.**
   - `CteBuilderImpl` -- public implementation of `ICteBuilder` / `IAnnotatableBuilderInternal`. Captures `Name` and annotation bag set by fluent `.HasName()` / provider-extension `.SetAnnotation()` calls.
   - `CteAnnotationsContainer` -- immutable snapshot of the `CteBuilderImpl` state placed into the expression tree as a query-cache key. Implements `IExpressionCacheKey` and `IEquatable<CteAnnotationsContainer>`; equality is string-ordinal on the `_cacheKey` built from name + sorted annotations. This ensures distinct `AsCte(b => ...)` configurations produce distinct cache entries even when delegate equality is reference-equal.
   - `TableBuilder.CteTableContext` partial -- builds `CteContext` (deferred) or looks up an existing one via `FindRegisteredCteContext`, then wraps it in a `CteTableContext`.
   - `CteContext` fields are now backed by `SqlCteField` ([SQL-AST]) instead of bare `SqlField` (`CteContext.cs:42`); `CteTableContext` fields are `SqlCteTableField` referencing back to the owning `SqlCteField`. `CteContext.RegisterField` reorders `CteClause.Fields` to match the inner query column order (`CteContext.cs:259-267`) so field/column positions stay aligned after incremental registration -- see Subsystem 32 for the new `RegisterVirtualField` entry point this backs.

10. **LoadWith / association subsystem.**
    - `AssociationHelper` -- static factory for association query lambdas (`CreateAssociationQueryLambda`).
    - `LoadWithEntity` / `LoadWithMember` -- tree nodes tracking `LoadWith` configuration levels. Both implement `Equals`/`GetHashCode`: `LoadWithEntity.Equals` recursively compares `MembersToLoad` lists; `LoadWithMember.Equals` compares by `MemberInfo` identity only (filter expressions are not part of equality).
    - `ILoadWithContext` / `ITableContext` -- interfaces added to `IBuildContext` implementations that support `LoadWith` root attachment.
    - `EagerLoading` -- minimal static class with `GetEnumerableElementType(Type, MappingSchema)`.

    Association via realized concrete type (PR #5511 / issue #5510): `IsAssociationInRealization` (`ExpressionBuilder.Associations.cs:49`) has three branches. The third branch handles the case where `member.ReflectedType != expression.Type` OR `member.DeclaringType != expression.Type`: it looks up the member on `expression.Type`, then calls `MappingSchema.GetAttribute<AssociationAttribute>(expression.Type, newMember)` directly using the realized concrete type as the owner type. After a projection such as `Select(x => x.Customer)` the member ReflectedType may already match `expression.Type` while `DeclaringType` still points at an unregistered abstract base (e.g. an EF Core `IEntityTypeConfiguration`-based base class). Using `expression.Type` directly avoids `EFCoreMetadataReader` returning nothing for the unregistered base.

    TPH discriminator narrowing on the association join (fuller PR #5661 writeup in Subsystem 25): `AssociationHelper.CreateAssociationQueryLambda` / `BuildAssociationQuery` now take a `parentAlreadyFiltered` bool. When `true` (the parent rows are already narrowed to a concrete TPH type via `OfType`/`Cast`), the discriminator predicate that would otherwise be added to the association join is skipped as redundant.

11. **Proxy / build-proxy subsystem.**
    - `IBuildProxy` / `BuildProxyBase<TOwner>` -- interface and generic abstract base for build proxies. `BuildProxyBase` implements `IBuildContext.MakeExpression` by mapping member/`ContextRefExpression` paths through the inner `BuildContext`, then routing results to `HandleTranslated` (for `SqlPlaceholderExpression` leaves) or `ProcessTranslated` (for composite expressions). `ProcessTranslated` handles these expression shapes: `ContextRefExpression` (creates a new proxy), `MemberExpression`, `MethodCallExpression` (via `ParseGenericConstructor`), `SqlGenericConstructorExpression` (recursively processes assignments and parameters), `NewExpression`/`MemberInitExpression` (via `ParseGenericConstructor`), `SqlPlaceholderExpression` (calls `HandleTranslated`), `Convert`/`ConvertChecked` unary nodes, `SqlAdjustTypeExpression`, and `SqlDefaultIfEmptyExpression`. `BuildProxyBase` implements `Equals(object?)` and `GetHashCode()` comparing `OwnerContext`, `BuildContext`, `OwnerContextRef`, `CurrentPath`, and `InnerExpression`.

12. **Entity construction / column mapping.**
    - `EntityConstructorBase` -- abstract base shared by `TableContext` and other contexts that need to build full-entity `SqlGenericConstructorExpression` trees. `BuildGenericFromMembers` iterates `ColumnDescriptor` collections, respects `SkipOnInsert` / `SkipOnUpdate` / `SkipOnEntityFetch` flags, handles nested dotted member names. See Subsystem 25 for the PR #5661 TPH sibling-column de-dup and abstract-intermediate per-subtype constructor additions.

13. **`SequenceHelper` utilities.** Static class with context-manipulation helpers: `PrepareBody`, `IsSameContext` / `CreateRef` / `CorrectExpression` / `CorrectTrackingPath`, `UnwrapProxy`, `FindError`, `CreateSpecialProperty`. This delta adds `IsSpecialProperty(Expression, IBuildContext)` and `ChangeSpecialPropertyObject(Expression, IBuildContext)` overloads (`SequenceHelper.cs:1037,1050`) -- rebase a special-property `MemberExpression` onto a different owning context, used by `CteTableContext.RegisterVirtualField` to move a virtual-field marker from `CteContext` onto the wrapping `CteTableContext`. `GetTableOrCteContext` is complemented by a narrower `GetTableContext` (returns only plain `ITableContext`, not CTE contexts) used where CTE targets are explicitly unsupported (e.g. `UpdateBuilder` UPDATE ... FROM join detection, `QueryExtensionBuilder` table hints).

14. **Parameter binding (`ParametersContext`).** Owns `CurrentSqlParameters` (the ordered `ParameterAccessor` list wired into the compiled delegate), `BuildParameter` (the main SQL-parameter allocation entry point), `RegisterDynamicExpressionAccessor`, `SimplifyConversion`. See Key types for the new `ReplaceDynamicColumnGetters` step added to `BuildParameter` this delta.

15. **AsQueryable parameterization API (PR #5495).** New public API in `LinqToDB.Linq` namespace:
    - `LinqExtensions.AsQueryable<TElement>(IEnumerable<TElement> source, IDataContext dataContext, Expression<Func<IAsQueryableBuilder<TElement>, IAsQueryableExceptBuilder<TElement>>> configure)` (`Source/LinqToDB/LinqExtensions/LinqExtensions.AsQueryable.cs:41`) -- the 3-arg overload. Wraps `source` in `Expression.Constant`, the context in `SqlQueryRootExpression.Create`, and `configure` in `Expression.Quote`; normal `ExpressionQueryImpl<T>` dispatch follows.
    - `EnumerableBuilder.BuildConfigured` validates the source can be evaluated on the client, unwraps the configure lambda, calls `TryParseConfigure` to walk the method-call chain, allocates the `SqlParameter` via `BuildParameter(..., InPredicate)`, constructs `EnumerableParameterizationConfig`, and creates `EnumerableContext` with that config.
    - `EnumerableBuilder.TryParseConfigure` walks a `MethodCallExpression` chain on `configureLambda.Body`. Recognized calls: `Parameterize()`, `Inline()`, `Except(nae)`.
    - This delta relaxes `EnumerableBuilder.BuildSequence`s `Parent == null` check on a raw `NewArrayExpression` (previously an immediate `Error`) to fall through to the client-evaluated parameter-array path when there is no parent context (`EnumerableBuilder.cs:59-73`) -- needed because `UpsertBuilder.BuildAsMerge` synthesizes a `Merge.Using(new T[] { item })` array literal with no enclosing parent.

16. **Aggregation subsystem (PR #5502 + PR #5557).**
    - `ExpressionBuilder.AggregationContext` nested sealed class -- implements `IAggregationContext`. Properties: `RootContext`, `ValueParameter`, `FilterExpressions`, `ValueExpression`, `Items` (captured-collection path), `OrderBy`, `IsDistinct`, `IsGroupBy`, `IsEmptyGroupBy`, `SqlContext`. `TranslateExpression` builds a `SqlPlaceholderExpression` from any expression via the current `SqlContext`.
    - `ExpressionBuilder.BuildAggregationFunction` (`ExpressionBuilder.Aggregation.cs:483`) -- aggregate path for a `ContextRefExpression` source. Builds an `AggregationContext`, calls `functionFactory`, wraps via `WrapAggregateResult`.
    - `ExpressionBuilder.BuildArrayAggregationFunction` (`ExpressionBuilder.Aggregation.cs:129`) -- the "plain array" path for a captured `NewArrayExpression` collection literal.
    - `WrapAggregateResult` (`ExpressionBuilder.Aggregation.cs:313`) -- for `info.IsGroupBy`, applies `sqlRewriter` eagerly and passes `sqlRewriter = null` to the lifter; for non-grouped aggregates the rewriter is deferred to lift time inside `AggregateExecuteContext.CreateWeakOuterJoin`.
    - `AggregateExecuteBuilder.BuildMethodCall` extracts `translatedPlaceholder`/`materializationCheck`/`sqlRewriter` from a `SqlAggregateLifterExpression`. This delta additionally wraps the sub-sequence build in `builder.IsolateOrderBy()` (`AggregateExecuteBuilder.cs:43`; see Subsystem 33) so an aggregate own OrderBy does not leak into outer CteUnion ordering state.
    - `AggregateExecuteContext.CreateWeakOuterJoin` (`AggregateExecuteBuilder.cs:204`) applies `SqlRewriter(Placeholder)` once, after `UpdateNesting`.
    - `BuildAggregationFunctionResult.Skipped()` sentinel lets `AggregateFunctionBuilder.Build(isExpression: true)` return `null` to cascade fallback.
    - `AggregateFunctionBuilder.ModeConfig` additions: `IsServerSideOnly`, `ItemTransform`, `ValueTransform`.

17. **Update / SET subsystem (PR #5506).**
    - `UpdateBuilder.InitializeSetExpressions.ApplyConversions.NeedsConversion` (`UpdateBuilder.cs:536`) -- returns `false` for `SqlParameter`, `SqlValue`, `SqlColumn`, `SqlFieldBase` (widened this delta from `SqlField` to also cover `SqlCteField`/`SqlCteTableField`), `SqlAnchor`. For `SqlExpression`/`SqlFunction`: scans `Parameters` to detect a self-reference to the target column and returns `false` when found (already a server-side transform of the stored value). Prevents `ValueConverter.ToProviderExpression` double-application on expressions like a `jsonb_set(...)` call that receives and returns the same storage type.

18. **`ExpressionBuildVisitor` CTE-context cloning fix.**
    - `ExpressionBuildVisitor.Clone(CloningContext)` (`ExpressionBuildVisitor.cs:79`) also copies `_cteContexts` into the cloned visitor, correcting expression keys via `cloningContext.CorrectExpression` and cloning context values via `cloningContext.CloneContext`. Previously `_cteContexts` was left null in the clone, so `FindRegisteredCteContext` would miss entries during subquery reuse. Field is `Dictionary<Expression, CteContext>?` with `ExpressionEqualityComparer.Instance`.

19. **Misc helpers.**
    - `ProjectFlagExtensions` -- `[MethodImpl(AggressiveInlining)]` predicates over `ProjectFlags` bits.
    - `ProjectionPathHelper` -- walks a `SqlGenericConstructorExpression` tree.
    - `EvaluationHelper` -- static `EvaluateExpression(expression, dataContext, parameterValues)`.
    - `BuildContextDebuggingHelper` -- `GetContextInfo` reads `tc.NamedTable.SourceID` (was `tc.SqlTable.SourceID`) for `TableContext`.
    - `ExposeExpressionVisitor` -- drops its bespoke `VisitBlock` F#-block-simplification override this delta (superseded, no longer needed with current F# codegen) and threads a new `IConvertContext` instance through every `IMemberConverter.Convert(node, convertContext, out handled)` call (signature widened -- see [SQL-PROVIDER]/[INTERNAL-API] for `IMemberConverter`).

20. **.NET 8+ only builders.**
    - `DistinctByBuilder` (`#if NET8_0_OR_GREATER`) -- `[BuildsMethodCall(nameof(Queryable.DistinctBy))]`, 2-argument form. Extracts a preceding `OrderBy` (via `WindowFunctionHelpers.ExtractOrderByPart`, or -- since this delta -- via the captured `ExpressionBuilder.CurrentOrderBy` state after building the source sequence inside an isolated OrderBy scope) and falls through via one of three strategies: on `SqlProviderFlags.IsDistinctOnSupported` (PostgreSQL/DuckDB), lowers directly to `SELECT DISTINCT ON (...)` (`ApplyDistinctOn`, `DistinctByBuilder.cs:190`); when `IsWindowFunctionsSupported`, rewrites via `ROW_NUMBER() OVER (PARTITION BY ...)` (`BuildDistinctByViaRowNumber<T>`), then re-applies the captured OrderBy on the outer result; when `IsOuterApplyJoinSupportsCondition` and not a subquery, rewrites as CROSS/OUTER APPLY with an inner `Where + Take(1)`. Falls through to `NotSupported()` otherwise. See Subsystem 27 for the DISTINCT ON path.
    - `MinByMaxByBuilder` (`#if NET8_0_OR_GREATER`) -- `MinBy`/`MaxBy` -> `OrderBy[Descending](selector).First[OrDefault]()`, preserving a preceding `OrderBy` as `ThenBy`/`ThenByDescending`.
    - `SetOperationByBuilder` (`#if NET8_0_OR_GREATER`) -- `ExceptBy`/`UnionBy`/`IntersectBy`, 3-argument form, requires `IsWindowFunctionsSupported`; rewrites set membership via `ROW_NUMBER() OVER (PARTITION BY keySelector)`.

21. **`OrderByBuilder` IComparer guard.**
    - `OrderByBuilder.CanBuildMethod` rejects BCL `OrderBy`/`ThenBy` overloads carrying an `IComparer<TKey>` argument, falling through to client-side evaluation instead of silently ignoring the comparer.

22. **Fluent Upsert + Insert/Update entity API (PR #5482, issue #2558).** Three entry points synthesize existing DML shapes and re-enter `TryBuildSequence` rather than building a bespoke statement pipeline:
    - `table.Insert(item, cfg => ...)` / `table.Update(item, cfg => ...)` (`EntityInsertBuilder`, `EntityUpdateBuilder`) synthesize the classic single-arg `Insert<T>(Expression<Func<T>>)` / predicate+setter `Update<T>(pred, setter)` calls via `EntitySetterBuilder`, so all downstream statement-building (`InsertBuilder`, `UpdateBuilder`) is unchanged.
    - `table.Upsert(item | items | queryable, cfg => ...)` (`UpsertBuilder`) picks native `SqlInsertOrUpdateStatement` vs. a synthesized `Merge` chain based on the parsed `UpsertConfig` (bulk source, `SkipInsert`, `InsertWhen`, or a non-PK `.Match` all force MERGE lowering -- see Key types). The native path builds `stmt.Insert.Items`/`stmt.Update.Items` directly via `UpdateBuilder.SetExpressionEnvelope` + `UpdateBuilder.InitializeSetExpressions`, matching PK columns from `table.GetKeys(false)` against the insert setter to populate `stmt.Update.Keys`.
    - `UpsertContext.SetRunQuery` (`UpsertBuilder.cs:742`) decides at query-run-setup time between `QueryRunner.SetNonQueryQuery` (native single-statement execution) and `QueryRunner.MakeAlternativeInsertOrUpdate` (UPDATE-then-INSERT-if-no-rows-affected emulation), gated by `SqlProviderFlags.IsInsertOrUpdateSupported`, the `.IsInsertOrUpdateWithPredicateSupported` flag (needed only when `.Update(v => v.When(...))` set `Statement.UpdateWhere`), and `.IsInsertOrUpdateRequiresAlignedBranches` (SAP HANA native `UPSERT ... WITH PRIMARY KEY` shares one VALUES list across both branches -- `HasDivergentInsertOrUpdateBranches` / `MatchKeysDivergeFromPrimaryKey` detect when that native form cannot honor the requested per-branch shape). `DataOptions.LinqOptions.UpsertEmulationPolicy == UpsertEmulationPolicy.Throw` turns a would-be-silent emulation fallback into a thrown `LinqToDBException` instead.
    - `InsertOrUpdateBuilder` (the pre-existing legacy `InsertOrUpdate` API) reuses `UpsertBuilder.HasDivergentInsertOrUpdateBranches`/`MatchKeysDivergeFromPrimaryKey` for the identical SAP HANA emulation decision (`InsertOrUpdateBuilder.cs:43-58`), so the new Upsert API and the legacy API share one emulation-detection code path.

23. **Eager-loading strategy dispatcher (PR #5450) -- `Default` / `KeyedQuery` / `CteUnion` fallback chain.** `ExpressionBuilder.CompleteEagerLoadingExpressions` (`ExpressionBuilder.EagerLoad.cs:557`) replaces the old single-strategy eager-load processing with a retry loop: `ResolveStrategy` (`:536`) picks the effective `EagerLoadingStrategy` from `TranslationModifier.EagerLoadingStrategy ?? DataOptions.LinqOptions.DefaultEagerLoadingStrategy` (remapping `CteUnion` to `KeyedQuery` when the provider lacks CTE or window-function support), then attempts to resolve every `SqlEagerLoadExpression` in the projection with that strategy inside a fresh per-attempt `EagerLoadState`. If any load fails (a strategy entry point returns `null` and records an `EagerLoadFallbackReason`), the whole attempt (including any preambles it queued) is discarded and the loop retries one step down the chain `CteUnion -> KeyedQuery -> Default` (`Default` must never fail -- it throws `InvalidOperationException` otherwise). On success the state is committed to `ExpressionBuilder._eagerLoadState`, consumed later by `BuildQuery<T>` (`ExpressionBuilder.cs:296-312`) to choose between `SetRunQueryWithCteUnion`, `SetRunQueryWithKeyedQueryBuffer`, or the plain `sequence.SetRunQuery`.
    - **Default** (`ExpressionBuilder.EagerLoadDefault.cs`, `ProcessEagerLoadingExpression`) is the pre-existing behavior renamed/extracted verbatim: `SELECT DISTINCT` on the parent joined to child rows via `SelectMany`, executed as a keyed preamble query.
    - **KeyedQuery** (`ExpressionBuilder.EagerLoadKeyedQuery.cs`, `ProcessEagerLoadingKeyedQuery`, `:40`) buffers the main query results, extracts distinct parent keys client-side via a `KeyedQueryKeysHolder<TKey>` (deterministically sorted so repeat executions produce a stable `VALUES` key order -- fixes issue #5665 direct-vs-remote SQL divergence), and issues one batched child query joined against those keys. Bails (`EagerLoadFallbackReason.ComplexParentReference`) when a parent dependency appears outside a simple equality/comparison filter predicate (e.g. inside `Contains`/`Any` on a parent collection) -- such references cannot be expressed as `VALUES` keys.
    - **CteUnion** (`ExpressionBuilder.EagerLoadUnion.cs`, `ProcessCteUnionBatch`, `:27`) batches every eager load marked `WithUnionLoadStrategy` at the same level into a single `UNION ALL` query: each branch becomes a CTE with a wide `ValueTuple` carrier row (reusing column slots across branches with matching nullable CLR types), and a parent CTE with `ROW_NUMBER() OVER (ORDER BY ...)` (built from the snapshotted user-visible `CurrentOrderBy`, see Subsystem 33) drives correlation. Rejects (`NestedBatchNotSupported`) when `previousKeys` is non-empty (a nested CTE batch cannot correlate to the outer level) and (`MaxColumnCountExceeded`) when the carrier `ValueTuple` would exceed the provider column limit.
    - Shared plumbing: `GenerateKeyExpression` (`ExpressionBuilder.EagerLoad.cs:93`) builds a possibly-nested `ValueTuple` key from N dependency expressions; this delta fixes an off-by-one in the 8+-member nesting branch (`count >= ValueTupleTypes.Length`, was `>`; recursion index `startIndex + count - 1`, was `startIndex + count`) that previously crashed on 8+-member composite keys. `CollectOrderBy`/`RemapOrderByThroughSelect` (`:151-266`) now remap an entity-level `OrderBy` lambda through intervening `Select` projections so it still resolves against the projected shape used by the KeyedQuery/Default detail selector.

24. **Named (multiple) query filters (PR #5525), mirroring the EF Core 10 API.** `EntityDescriptor.QueryFilters` (a list of `EntityQueryFilter { FilterKey, FilterLambda, FilterFunc }`, replacing the old single `QueryFilterLambda`/`QueryFilterFunc` pair -- [MAPPING]) lets a mapped type register several independently-toggleable filters. `TableBuilder.ApplyQueryFilters` (`TableBuilder.cs:95`) iterates `allFilters`, drops any whose key is disabled via `builder.IsFilterDisabled(entityType, entry.FilterKey)`, and Wheres in only the remaining `activeFilters`; the simple (no dynamic `FilterFunc`) path chains one `Where` per active filter via the new `BuildFilterLambda` helper (`:206`), which additionally adapts an interface/base-typed filter-lambda parameter (e.g. `Expression<Func<ISoftDelete, IDataContext, bool>>`) to the concrete `entityType` via `Expression.Convert` so `Queryable.Where<entityType>` accepts it. `IgnoreFiltersBuilder` gains a second dispatch for `Methods.LinqToDB.IgnoreFiltersByKey` (the `.IgnoreFilters(keys, types)` overload), pushing a `FilterIgnoreScope(keys, types)` via `builder.PushDisabledQueryFilters(string[], Type[])` (new overload, `ExpressionBuilder.SqlBuilder.cs:130`) alongside the existing type-only `PushDisabledQueryFilters(Type[])`.

25. **TPH inheritance fixes (PR #5661) -- abstract intermediates, sibling columns, derived-type associations.**
    - Sibling-column de-dup: `EntityConstructorBase.BuildGenericFromMembers` (`EntityConstructorBase.cs:78`) now caches the projected value expression per distinct `(ColumnDescriptor.ColumnName, MemberType, ValueConverter reference)` shape in a `sharedColumns` dictionary, so two TPH sibling subtypes whose distinct C# members map to the identical physical column collapse onto a single SELECT column instead of duplicating it once per subtype; `BuildFullEntityExpressionInternal` additionally concatenates `EntityDescriptor.InheritanceSiblingColumns` ([MAPPING]) into the columns passed to `BuildGenericFromMembers` so each concrete TPH type can see its own physical column even when a base type same-named member maps elsewhere.
    - Abstract intermediate constructors: when `OfType<TIntermediate>()` targets an abstract TPH type with no `InheritanceMapping` of its own, `BuildFullEntityExpressionInternal` (`EntityConstructorBase.cs:606-`) now walks up to the hierarchy root to find its `InheritanceMapping`, filters to subtypes assignable to the intermediate, and builds a dedicated per-subtype constructor (`perSubtypeConstructor` flag) against the hierarchy root rather than the intermediate own (columnless) constructor.
    - Association discriminator narrowing: `AssociationHelper.CreateAssociationQueryLambda`/`BuildAssociationQuery` resolve the parent hierarchy root from `parentType` (not the unreliable `parentOriginalType`) and OR every inheritance code mapped to `parentType` into the join discriminator predicate (a CLR type can carry several discriminator codes) -- skipped entirely when the new `parentAlreadyFiltered` flag is set (parent rows already narrowed via `OfType`/`Cast`, tracked by `TableBuilder.TableContext.FilteredByOfType`, populated by `OfTypeBuilder.BuildMethodCall` calling `table.AddFilteredByOfType(objectType)`). Known limitation retained: an association declared on an abstract intermediate type still emits no discriminator filter for that intermediate own leaf codes.

26. **Window Functions -- new `Sql.Window` API (PR #5468), `WindowFunctionsMemberTranslator` rewrite.** `Source/LinqToDB/Linq/Translation/WindowFunctionsMemberTranslator.cs` grows from a thin registration surface to a ~1600-line public translation base class with ~50 `virtual Expression? Translate*(ITranslationContext, MethodCallExpression, TranslationFlags)` methods, one per `Sql.Window.*` analytic function: ranking (`RowNumber`, `Rank`, `DenseRank`, `PercentRank`, `CumeDist`, `NTile`), distribution (`PercentileCont`, `PercentileDisc`, windowed variants, hypothetical-set `HypotheticalRank`/`HypotheticalDenseRank`/`HypotheticalPercentRank`/`HypotheticalCumeDist`), aggregate-as-window (`Sum`, `Average`, `Min`, `Max`, `RatioToReport`, `Median`), statistical (`StdDev`/`StdDevPop`/`StdDevSamp`, `Variance`/`VarPop`/`VarSamp`, `CovarPop`/`CovarSamp`/`Corr`/`RegrSlope`/`RegrIntercept`/`RegrCount`/`RegrR2`/`RegrAvgX`/`RegrAvgY`/`RegrSXX`/`RegrSYY`/`RegrSXY`), `Count`, and offset/value functions (`Lead`, `Lag`, `FirstValue`, `LastValue`, `NthValue`). Each `virtual` method is overridable per provider (mirrors the version-aware-subclass pattern in `code-design.md`); shared machinery lives in `CollectWindowFunctionInformation`, `TranslateWindowFunction`, `TranslateOrderItems`, `TranslatePartitionBy`, `TranslateWindowFunctionMultiArg`.
    - `ITranslationContext.ProviderFlags` (new property, `Source/LinqToDB/Linq/Translation/ITranslationContext.cs:29`) exposes the new `TranslationProviderFlags` (see Key types) so a translator can branch on `DefaultNullsOrdering`/`IsNullsOrderingSupported` without needing the full `SqlProviderFlags` surface; `ExpressionBuildVisitor.ProviderFlags` and its nested `TranslationContext.ProviderFlags` both forward to `Builder.TranslationProviderFlags`.

27. **DISTINCT ON via DistinctBy (PR #5630, PostgreSQL/DuckDB).** New `SqlProviderFlags.IsDistinctOnSupported` ([SQL-PROVIDER]) routes `DistinctByBuilder` to `ApplyDistinctOn` (`DistinctByBuilder.cs:190`) instead of the `ROW_NUMBER()` emulation: the key selector becomes the `ON (...)` list (via `ExpressionBuilder.CollectDistinctPlaceholders`, non-constant SQL expressions only) and the existing `SelectQuery.OrderBy` is reordered so those keys lead (synthesizing an entry for any ON key not already present), then `SelectQuery.Select.IsDistinct = true` / `.DistinctOn = onExpressions` is set for the SQL builder to emit. Falls through to the pre-existing `ROW_NUMBER()`/OUTER-APPLY strategies when the key has no usable SQL form (e.g. `DistinctBy(x => 1)`, an all-constant key).

28. **`PreferClientCalculation` option (PR #5604 follow-up).** `ExpressionBuildVisitor.PreferClientCalculation(Expression)` (`ExpressionBuildVisitor.cs:2144`) gates the projection-time client/server split: true only in `BuildPurpose.Expression`, outside `BuildFlags.ForSetProjection`, when `DataOptions.LinqOptions.PreferClientCalculation` is on and neither `Builder.PreferServerSide` nor `Builder.IsServerSideOnly` forces server evaluation. Consulted from `VisitMethodCall`, `VisitUnary`, `VisitBinary`, and `TryConvertToSql`. `MappedFunctionAllowsClientCalculation(MethodInfo)` restricts the opt-out to methods carrying an `Sql.ExpressionAttribute` (mapped functions) -- structural LINQ methods (aggregates, etc.) have no such attribute and always keep translating to SQL regardless of the option.

29. **MemberTranslator wins over built-in predicate translation (PR #5348, issue #5347).** `ExpressionBuildVisitor.TryConvertPredicate` (was `ConvertPredicateMethod`, now returns a bool + out param instead of a same-node sentinel) first calls `TranslateMember(BuildContext, node, out translatedMember)` -- a user-registered `IMemberTranslator` -- before falling through to the built-in `Contains`/`Equals`/comparison predicate logic, so e.g. a custom `Contains` translation on a string-vs-jsonb column wins. `VisitMethodCall` now calls `TryConvertPredicate` unconditionally in every build purpose that reaches the boolean-method branch (not just Sql/Expression), fixing predicates inside generic helpers, MergeInto subqueries, and EF.Core query filters that previously survived untranslated and produced `WHERE NULL`.

30. **Enum-vs-null comparison emits IS [NOT] NULL (PR #5667, issue #5666).** `ExpressionBuildVisitor`'s enum-constant comparison path (around `ExpressionBuildVisitor.cs:4662`) now special-cases a `null` constant operand: instead of mapping it through `MappingSchema.GetMapValues` (which could coerce it to the enum underlying-type default, e.g. `0`, turning a null-check into a value filter that silently drops rows equal to that default), it emits `SqlPredicate.IsNull(operandSql, isNot: op == NotEqual)` directly.

31. **`Linq.Expressions` mapping registry made thread-safe (PR #5623).** `Source/LinqToDB/Linq/Expressions.cs` `Members` and `_binaries` are now `ConcurrentDictionary<string, ConcurrentDictionary<...>>` (were `Dictionary`, guarded ad hoc by a `Lock`); `MapMember`/`MapBinary` use `GetOrAdd`/indexer-assignment instead of `TryGetValue`+`Add`. XML-doc remarks on the public `MapMember`/`MapBinary` overloads were updated from "not thread safe, application-init only" to "registration is thread-safe; for predictable results configure during application initialization". Also in this delta: the local `string.CompareTo` member-mapping helper `ConvertToCaseCompareToImpl` (and its `[Sql.Extension(builderType: ConvertToCaseCompareToBuilder)]`) was removed from `Expressions.cs` -- issue #5613 migrated `string.CompareTo` translation to a proper member translator elsewhere ([SQL-PROVIDER]/[INTERNAL-API]), so the two `M(() => "".CompareTo(...))` registry entries are gone.

32. **CteContext virtual-field registration -- CteUnion support.** `CteContext.RegisterVirtualField(Expression)` (`CteContext.cs:315`) and `CteTableContext.RegisterVirtualField(Expression)` (`CteTableContext.cs:109`) resolve an arbitrary expression (e.g. a `ROW_NUMBER()` placeholder) into a stable special-property `MemberExpression` (`"$v[N]-<cteFieldName>"`) backed by a registered `SqlCteField`, so a later rebuild of the same logical expression against the CTE (as CteUnion phase-2 does when wiring nested branch correlation) resolves to the same field rather than re-adding a duplicate CTE column. `SequenceHelper.IsSpecialProperty(Expression, IBuildContext)` / `ChangeSpecialPropertyObject` (Subsystem 13) are the supporting primitives that let the virtual-field marker be rebased from `CteContext` onto the wrapping `CteTableContext`.

33. **OrderBy capture/isolation plumbing (`_currentOrderBy`, `IsolateOrderBy`, `RegisterOrderBy`, `ResolveOrderByItems`) -- CteUnion support.** `ExpressionBuilder` carries a `_currentOrderBy` list (`ExpressionBuilder.cs:156`) populated by `OrderByBuilder.BuildMethodCall` via `RegisterOrderBy(body, descending, nulls, reset)` (`:171`) every time an `OrderBy`/`ThenBy` clause is built, so eager-load strategies can recover the query user-visible ordering without re-parsing the expression tree. `IsolateOrderBy()` (`:222`, an `IDisposable` scope) saves and clears the list around any independent sub-sequence build (subquery, set-operation side, join inner, aggregate source, `HandleSubquery`) so a child ordering does not leak into the outer `CteUnion` parent-CTE `ROW_NUMBER() OVER (ORDER BY ...)`. `ResolveOrderByItems(IBuildContext)` re-resolves each captured body into a stable `SqlPlaceholderExpression` once the owning context is finalized (called from `CteContext.InitQuery`); `RemoveOrderByEntries(predicate)` drops entries that a subsequent `Distinct`/`SelectDistinct` projection cannot preserve (`DistinctBuilder.BuildMethodCall`).

34. **F# raw-SQL constructor materialization honors [Column]/[ValueConverter] (PR #5659, issue #4437); RecordReaderBuilder fast-mode reduce.** `RecordReaderBuilder.EntityConstructor.MakeAssignExpression` now reduces the `ConvertFromDataReaderExpression` against the sample reader immediately (`.Reduce(Builder.DataContext, Builder.Reader)`) instead of leaving a slow-mode node that dispatches through a `ColumnReader` per row, matching the main `QueryRunner` mapper path; the reader-index dictionary build is also hardened against duplicate/empty column names (a raw `select 1, 1` no longer throws on `ToDictionary`, keeping the first index per name). `BuildReaderFunction` reads the sequential-access flag from `DataContext.Options.LinqOptions.OptimizeForSequentialAccess` (was the old static `LinqToDB.Common.Configuration.OptimizeForSequentialAccess` -- see Subsystem 35) with `reduce: false` since the assign expressions are already reduced.

35. **`OptimizeForSequentialAccess` promoted to a context-level `LinqOptions` (PR #5639).** `RecordReaderBuilder` and other Tier-2 consumers switch from the process-wide static `LinqToDB.Common.Configuration.OptimizeForSequentialAccess` ([INFRA]) to `DataContext.Options.LinqOptions.OptimizeForSequentialAccess`, letting the setting vary per `DataOptions`/context rather than being a single global flag.

## Interactions

- **Driven by [`LINQ`](../LINQ/INDEX.md):** `Query<T>.GetQuery` / `ExpressionQuery` create an `ExpressionBuilder` and call `Build<T>` once per cache miss.
- **Consumes [`SQL-AST`](../SQL-AST/INDEX.md):** every builder mutates / constructs `SelectQuery`, `SqlField`, `SqlBinaryExpression`, `SqlPlaceholderExpression`, `SqlInsertStatement`, etc. This delta adds `SqlCteField` / `SqlCteTableField` (new SQL-AST node types backing `CteContext`/`CteTableContext` field registration, replacing bare `SqlField` reuse) and `ISqlNamedTable` (the common interface `SqlTable`/`SqlCteTable` implement, now returned by `ITableContext.NamedTable`).
- **Hands off to [`SQL-PROVIDER`](../SQL-PROVIDER/INDEX.md):** finalized `SqlStatement` flows into `ISqlOptimizer.Finalize` then `ISqlBuilder` for emission. New `SqlProviderFlags` members consumed this delta: `IsUpsertWithMergeLoweringSupported`, `IsUpsertMergeWithPredicateSupported`, `IsInsertOrUpdateRequiresAlignedBranches`, `IsInsertOrUpdateWithPredicateSupported`, `IsDistinctOnSupported`, `DefaultNullsOrdering`/`IsNullsOrderingSupported` (surfaced via the new `TranslationProviderFlags`).
- **Uses [`MAPPING`](../MAPPING/INDEX.md):** `EntityDescriptor` lookups for query filters, column mapping for materialization, association definitions. This delta adds `EntityDescriptor.QueryFilters` (list, replacing the single `QueryFilterLambda`/`QueryFilterFunc` pair) and `EntityDescriptor.InheritanceSiblingColumns` (TPH sibling columns).
- **Uses [`EXPR`](../EXPR/INDEX.md):** `ContextRefExpression`, `SqlPlaceholderExpression`, `SqlGenericConstructorExpression`, `SqlAggregateLifterExpression` etc. live in `LinqToDB.Internal.Expressions`. `MarkerExpression(MarkerType.ExplicitEagerLoad)` (new marker type this delta) tags genuine `LoadWith`/`ThenLoad` collection loads for `ImplicitEagerLoadGuardVisitor`.
- **Uses [`INFRA`](../INFRA/INDEX.md):** `ObjectPool`, `ActivityService` metrics, common helpers.
- **Generated by [`CODEGEN`](../CODEGEN/INDEX.md):** `BuildersGenerator` emits `ExpressionBuilder.g.cs` containing `FindBuilderImpl`.
- **New cross-area `DataOptions.LinqOptions` consumed this delta** (definition outside this area -- likely CORE/INFRA, not read in this pass): `EagerLoadingStrategy`/`DefaultEagerLoadingStrategy`, `ImplicitCollectionLoading`, `PreferClientCalculation`, `UpsertEmulationPolicy`, `OptimizeForSequentialAccess` (promoted from the old static `Common.Configuration.OptimizeForSequentialAccess`).

## Inbound / outbound dependencies

- **Inbound:** `LINQ` (`Internal/Linq/Query.cs`, `Internal/Linq/ExpressionQuery.cs`) constructs `ExpressionBuilder`. `EXPR` types are referenced inbound via `ContextRefExpression.BuildContext`.
- **Outbound:** `LinqToDB.Internal.SqlQuery.*` (heavy, now including `SqlCteField`/`SqlCteTableField`/`ISqlNamedTable`), `LinqToDB.Internal.Expressions.*` (including `SqlAggregateLifterExpression`), `LinqToDB.Internal.Conversion`, `LinqToDB.Internal.DataProvider.Translation` (`IMemberTranslator`, now called with an added `IConvertContext` parameter), `LinqToDB.Mapping` (`EntityDescriptor.QueryFilters`, `InheritanceSiblingColumns`), `LinqToDB.Metrics`, `LinqToDB.Internal.Reflection.Methods` (well-known method handles, including the new `Merge.*`/`Update.UpdatePredicateSetter`/`Insert.FromTable.Insert` handles consumed by `UpsertBuilder`/`EntityInsertBuilder`/`EntityUpdateBuilder`). Public translation layer: `LinqToDB.Linq.Translation.AggregateFunctionBuilder`, `BuildAggregationFunctionResult`, `WindowFunctionsMemberTranslator`, `TranslationProviderFlags` (new). Public root-namespace: `LinqToDB.EagerLoadingStrategy` (new, outside area globs).

## Known issues / debt

- Source-generator dispatcher relies on every builder declaring its triggering attributes correctly; a missing attribute is a silent miss (the call falls through to `null` and surfaces as `BuildSequenceResult.NotSupported`).
- Many `*Builder.BuildMethodCall` methods carry `[BuildsMethodCall]` lists that include legacy method names alongside current ones (`OrderBy` + `ThenOrBy`, `Insert` + `InsertWithOutput*`); changes here can affect provider compatibility.
- `IBuildContext` has a `// TODO: probably not needed` on `Parent.set`.
- `BuildInfo` is a mutable bag with `set;` accessors; its `IsSubQuery` is derived from `Parent != null` rather than tracked explicitly.
- `MethodCallParser.cs` referenced in `kb-areas.md` does not exist -- the area registry should be updated to remove it.
- `MergeBuilder.UpdateWhenMatched` silently skips the entire Update operation when no updatable non-key columns are found with an implicit setter (workaround for issue #2843).
- `TableLikeQueryContext` `ProjectionHelper<,>` helper type uses a `selft_target` property name (typo for `self_target`) -- a cosmetic issue but visible in reflection-based diagnostics.
- `ExpressionBuildVisitor` carries both `_translationCache` and `_columnCache` as mutable `SnapshotDictionary` fields; the `Clone(CloningContext)` path copies only entries whose keys reference cloned contexts.
- `CteContext.InitQuery()` uses a `_isRecursiveCall` flag to guard against re-entrant initialization but does not hold a lock -- concurrent compilation of the same CTE from multiple threads would produce undefined behavior. Currently safe because `ExpressionBuilder` is not shared.
- `EnumerableBuilder.BuildConfigured` rejects sources that reference outer query state with a runtime error at query-build time rather than a compile-time diagnostic.
- `IAsQueryableBuilder<T>` and `IAsQueryableExceptBuilder<T>` methods are marker-only; calling them outside an expression context is undefined behaviour and the implementations are not provided (the interfaces have no concrete class backing them in the shipped assembly). The same caveat now applies to `IUpsertSpec<T>`/`IUpsertInsertSpec<T>`/`IUpsertUpdateSpec<T>`/`IEntityInsertSpec<T>`/`IEntityUpdateSpec<T>` (PR #5482).
- `UpdateBuilder.NeedsConversion` applies `ValueConverter` wrapping only when `NeedsConversion` returns true. The `SqlExpression`/`SqlFunction` column-reference check uses `ReferenceEquals` on `ColumnDescriptor` objects -- if a descriptor is not the identical instance (e.g. after mapping schema rebuild), the check may produce a false positive, re-applying the converter.
- `AggregateFunctionBuilder.Build` with `isExpression: true` and `!IsServerSideOnly` can return `null` (lenient bail-out) -- callers that do not handle `null` will throw a `NullReferenceException` rather than getting a SQL error. This is intentional design but undocumented in the public surface XML docs.
- `DistinctByBuilder`, `MinByMaxByBuilder`, `SetOperationByBuilder` are all `#if NET8_0_OR_GREATER` conditional -- on `net462`/`netstandard2.0` builds these operators are silently unavailable (fall through to `NotSupported` in the dispatcher because the class is not compiled). No runtime error surface for callers on older TFMs.
- `OrderByBuilder` now rejects `IComparer<TKey>` overloads -- callers that previously relied on silent acceptance (ignoring the comparer) will get a `NotSupported` result and likely a runtime exception.
- `LoadWithMember.Equals` compares only `MemberInfo` -- two `LoadWithMember` instances for the same member with different `FilterExpression` or `FilterFunc` compare as equal. This can cause unexpected deduplication in `LoadWithEntity.MembersToLoad` comparisons.
- `UpsertBuilder` Phase-3 MERGE lowering surfaces a build-time error (not a silent fallback) when the provider lacks `IsUpsertWithMergeLoweringSupported` / `IsUpsertMergeWithPredicateSupported` -- callers on such providers must reshape the call (drop conditional/skip-branch usage) rather than get an emulation.
- `ExpressionBuilder.LastEagerLoadFallbackChain` / `EagerLoadFallbackReason` (PR #5450) are diagnostic-only and not exposed through any public API -- a query that silently downgrades from `CteUnion` to `Default` has no built-in way for a caller to observe why.
- `ImplicitEagerLoadGuardVisitor` guard is keyed off a single `_inEagerLoadProcessing` bool on `ExpressionBuilder` -- correct only because eager-load preamble sub-builds are always synchronous/sequential within one `ExpressionBuilder` instance; would need re-entrancy tracking if that assumption changes.
- Association narrowing via `OfType`/`Cast` (`TableContext.FilteredByOfType`) only recognizes concrete-type discriminator matches; an association declared on an abstract intermediate TPH type still emits no discriminator filter for that intermediate own leaf codes (documented limitation in the `AssociationHelper.cs` comment, PR #5661).
- `EntityConstructorBase.BuildGenericFromMembers` new `sharedColumns` de-dup cache (PR #5661) is keyed by `column.ColumnName` (ordinal string) plus `(MemberType, ValueConverter reference-equality)` -- two sibling TPH columns with the same name but a different `ValueConverter` instance that is semantically identical will not collapse onto one SELECT column (over-cautious, not incorrect).
- `ITableContext.SqlTable` was removed in favour of `NamedTable` (`ISqlNamedTable`) this delta -- any external/provider code referencing `ITableContext.SqlTable` directly (internal-only interface, so in-tree providers only) needs updating; `TableBuilder.TableContext.SqlTable` (the concrete property, not the interface member) is unaffected.
- `UpdateBuilder.NeedsConversion` skip-list now checks `SqlFieldBase` (widened from `SqlField`) -- this silently also covers `SqlCteTableField`/`SqlCteField`; not yet exercised by a CTE-target UPDATE test as of this delta.
- `PreferClientCalculation` (`ExpressionBuildVisitor.cs:2144`) is gated purely on the presence of `Sql.ExpressionAttribute` (`MappedFunctionAllowsClientCalculation`) -- a mapped function registered via the newer `AggregateFunctionBuilder`/`WindowFunctionsMemberTranslator` translation-context APIs (no attribute) is not eligible for client-side evaluation even when logically pure; not yet reconciled.

## See also

- [`SQL-AST`](../SQL-AST/INDEX.md) -- output target of every builder.
- [`SQL-PROVIDER`](../SQL-PROVIDER/INDEX.md) -- receives the finalized `SqlStatement`.
- [`LINQ`](../LINQ/INDEX.md) -- caller; also owns query caching.
- [`EXPR`](../EXPR/INDEX.md) -- `ContextRefExpression`, `SqlPlaceholderExpression`, `SqlAggregateLifterExpression`, visitor infrastructure.
- [`CODEGEN`](../CODEGEN/INDEX.md) -- `BuildersGenerator` Roslyn source generator.
- [`../../architecture/expression-translator.md`](../../architecture/expression-translator.md) -- narrative walkthrough.
- `.agents/docs/code-design.md` -- public-API + AST namespace invariants.

## Pointers

- Dispatcher template: `Source/CodeGenerators/BuildersGenerator.cs:165`.
- Per-query entry: `ExpressionBuilder.Build<T>` at `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.cs:124`.
- Recursion core: `TryBuildSequence` at `ExpressionBuilder.cs:336`.
- Reference operator implementations: `WhereBuilder.cs`, `SelectBuilder.cs`, `OrderByBuilder.cs`, `JoinBuilder.cs`, `GroupByBuilder.cs`, `InsertBuilder.cs`.
- Sequence root: `TableBuilder.cs`, `ContextRefBuilder.cs`, `EnumerableBuilder.cs`.
- Merge pipeline entry: `MergeBuilder.Merge.BuildMethodCall` -> `MergeBuilder.MergeContext`.
- CTE entry: `TableBuilder.CteTableContext.cs` partial -> `CteContext.InitQuery()`.
- Parameter binding: `ParametersContext.BuildParameter` / `CurrentSqlParameters`.
- Expression normalization: `ExposeExpressionVisitor.ExposeExpression` (called by `ExpressionBuilder.ExposeExpression`).
- AsQueryable parameterization entry: `EnumerableBuilder.BuildConfigured` / `TryParseConfigure` (`EnumerableBuilder.cs:103`).
- Association via concrete type: `ExpressionBuilder.AssociationToRealization` + `IsAssociationInRealization` (`ExpressionBuilder.Associations.cs:19`, `ExpressionBuilder.Associations.cs:49`).
- Per-column parameterization decision: `EnumerableParameterizationConfig.ShouldForceParameter` (`EnumerableParameterizationConfig.cs:35`).
- Aggregate pipeline (non-grouped): `ExpressionBuilder.BuildAggregationFunction` (`ExpressionBuilder.Aggregation.cs:483`) -> `AggregateExecuteBuilder.AggregateExecuteContext.CreateWeakOuterJoin` -> `SqlRewriter` hook.
- Aggregate pipeline (grouped): `WrapAggregateResult` (`ExpressionBuilder.Aggregation.cs:313`) applies `sqlRewriter` eagerly.
- Captured-collection aggregate: `ExpressionBuilder.BuildArrayAggregationFunction` (`ExpressionBuilder.Aggregation.cs:129`).
- COALESCE / null-check wrapper: `SqlAggregateLifterExpression` (`Source/LinqToDB/Internal/Expressions/SqlAggregateLifterExpression.cs`).
- ValueConverter skip-for-field: `UpdateBuilder.NeedsConversion` (`UpdateBuilder.cs:536`).
- CTE-context clone fix: `ExpressionBuildVisitor.Clone` (`ExpressionBuildVisitor.cs:119`).
- DistinctBy rewrite entry: `DistinctByBuilder.BuildMethodCall` -> `BuildDistinctByViaRowNumber` / `BuildDistinctByViaOuterApply` / `ApplyDistinctOn`.
- MinBy/MaxBy rewrite entry: `MinByMaxByBuilder.BuildMethodCall` (`MinByMaxByBuilder.cs:19`).
- Set-operation-by rewrite entry: `SetOperationByBuilder.BuildMethodCall` -> `BuildExceptBy` / `BuildIntersectBy` / `BuildUnionBy`.
- CteAnnotationsContainer cache-key: `CteAnnotationsContainer.BuildCacheKey` (`CteAnnotationsContainer.cs:45`).
- OrderByBuilder IComparer guard: `OrderByBuilder.CanBuildMethod` (`OrderByBuilder.cs:24-26`).
- Upsert entry: `UpsertBuilder.BuildMethodCall` (`UpsertBuilder.cs:36`) -> native path `UpsertContext` / MERGE path `UpsertBuilder.BuildAsMerge` (`UpsertBuilder.cs:244`).
- Entity Insert/Update entry: `EntityInsertBuilder.BuildMethodCall` (`EntityInsertBuilder.cs:35`), `EntityUpdateBuilder.BuildMethodCall` (`EntityUpdateBuilder.cs:37`).
- Eager-load strategy dispatch: `ExpressionBuilder.CompleteEagerLoadingExpressions` (`ExpressionBuilder.EagerLoad.cs:557`) -> `ResolveStrategy` (`ExpressionBuilder.EagerLoad.cs:536`).
- CteUnion batch entry: `ExpressionBuilder.ProcessCteUnionBatch` (`ExpressionBuilder.EagerLoadUnion.cs:27`).
- KeyedQuery entry: `ExpressionBuilder.ProcessEagerLoadingKeyedQuery` (`ExpressionBuilder.EagerLoadKeyedQuery.cs:40`).
- Load-strategy marker entry: `WithLoadStrategyBuilder.BuildMethodCall` (`WithLoadStrategyBuilder.cs:16`).
- Named query filter application: `TableBuilder.ApplyQueryFilters` (`TableBuilder.cs:95`) -> `TableBuilder.BuildFilterLambda` (`:206`); disable check `TranslationModifier.IsFilterDisabled(Type, string)`.
- Window-function translation surface: `WindowFunctionsMemberTranslator` (`Source/LinqToDB/Linq/Translation/WindowFunctionsMemberTranslator.cs:16`).
- OrderBy capture/isolation: `ExpressionBuilder.RegisterOrderBy` / `IsolateOrderBy` / `ResolveOrderByItems` (`ExpressionBuilder.cs:162,213,179`).
- Off-by-one fix (8+-member composite keys): `ExpressionBuilder.GenerateKeyExpression` (`ExpressionBuilder.EagerLoad.cs:93`).

<details><summary>Coverage</summary>

- Tier 1 (visited / total): 68 / 68
- Tier 2 (visited / total): 86 / 86

Read (prior runs -- build sha 4a478ff14):
- `EnumerableBuilder.cs` -- BuildConfigured + TryParseConfigure (PR #5495)
- `EnumerableContext.cs` -- _parameterization optional ctor param
- `EnumerableParameterizationConfig.cs` (new file -- PR #5495)
- `ExpressionBuilder.Associations.cs` -- AssociationToRealization + IsAssociationInRealization (PR #5511 / #5510)
- `ExpressionBuilder.SqlBuilder.cs` -- Project handling of Convert(body) wrapping
- `Source/LinqToDB/Linq/Expressions.cs` (LinqToDB.Linq, sampled for AsQueryable registration)
- `Source/LinqToDB/Linq/IAsQueryableBuilder.cs` (new -- PR #5495)
- `Source/LinqToDB/Linq/IAsQueryableExceptBuilder.cs` (new -- PR #5495)
- `Source/LinqToDB/LinqExtensions/LinqExtensions.AsQueryable.cs` -- 3-arg overload
- `Source/LinqToDB/Internal/Reflection/Methods.cs` -- AsQueryableConfigured registration at :205

Read (prior run -- delta sha 2e67bafc9):
- `Source/LinqToDB/Internal/Linq/Builder/AggregateExecuteBuilder.cs` -- AggregateExecuteContext now handles SqlAggregateLifterExpression (MaterializationCheck + SqlRewriter); CreateWeakOuterJoin applies SqlRewriter after UpdateNesting at OUTER APPLY lift time (PR #5502/#5557)
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuildVisitor.cs` (lines 79-133) -- Clone now copies _cteContexts alongside _associations/_translationCache/_columnCache (PR #5557 correctness fix)
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.Aggregation.cs` -- BuildArrayAggregationFunction (new method for captured-collection aggregates, PR #5557); WrapAggregateResult static helper (eager SqlRewriter for grouped, deferred for non-grouped); AggregationContext nested class
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.Associations.cs` -- IsAssociationInRealization third branch extended (PR #5548): guard now also fires when member.DeclaringType != expression.Type
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.cs` (lines 1-200) -- no net-new structural changes; confirmed Build<T> / FindBuilderImpl unchanged
- `Source/LinqToDB/Internal/Linq/Builder/UpdateBuilder.cs` -- NeedsConversion static helper (PR #5506): skips ValueConverter wrap for SqlParameter/SqlValue/SqlColumn/SqlField/SqlAnchor
- `Source/LinqToDB/Linq/Expressions.cs` (lines 1-80) -- MapMember overload surface; no aggregation-relevant changes in scanned range
- `Source/LinqToDB/Linq/Translation/AggregateFunctionBuilder.cs` -- IsServerSideOnly/ItemTransform/ValueTransform on ModeConfig; Build(isExpression) lenient bail-out path via Skipped()
- `Source/LinqToDB/Linq/Translation/BuildAggregationFunctionResult.cs` -- Skipped() sentinel factory and IsSkipped bool added (PR #5557)
- `Source/LinqToDB/Linq/Translation/WindowFunctionsMemberTranslator.cs` (lines 1-120) -- registration surface; no aggregate-pipeline changes in scanned range

Read (prior run -- delta sha b3340aa9):
- `Source/LinqToDB/Internal/Linq/Builder/BuildContextDebuggingHelper.cs` -- GetContextInfo handles ScopeContext and SubQueryContext; ContextId is #if DEBUG only
- `Source/LinqToDB/Internal/Linq/Builder/BuildProxyBase{TOwner}.cs` -- ProcessTranslated handles SqlAdjustTypeExpression and SqlDefaultIfEmptyExpression; Equals/GetHashCode added
- `Source/LinqToDB/Internal/Linq/Builder/CteAnnotationsContainer.cs` -- implements IExpressionCacheKey and IEquatable<CteAnnotationsContainer>; ordinal-sorted BuildCacheKey
- `Source/LinqToDB/Internal/Linq/Builder/DistinctByBuilder.cs` -- new #if NET8_0_OR_GREATER builder for Queryable.DistinctBy; ROW_NUMBER or OUTER APPLY rewrite
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuildVisitor.cs` -- _cteContexts is Dictionary<Expression, CteContext>? with ExpressionEqualityComparer.Instance; Clone copies it
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.Aggregation.cs` -- AggregationContext properties confirmed; TranslateExpression/SimplifyEntityLambda visible
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.SqlBuilder.cs` -- no structural changes (BuildWhere entry confirmed)
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.cs` -- no structural changes; FindBuilderImpl partial declaration confirmed
- `Source/LinqToDB/Internal/Linq/Builder/LoadWithEntity.cs` -- Equals/GetHashCode added; ToDebugString helpers
- `Source/LinqToDB/Internal/Linq/Builder/LoadWithMember.cs` -- Equals compares by MemberInfo only; ToDebugString FF/FE suffixes
- `Source/LinqToDB/Internal/Linq/Builder/MergeBuilder.On.cs` -- no structural changes
- `Source/LinqToDB/Internal/Linq/Builder/MergeBuilder.UpdateWhenMatched.cs` -- no structural changes (scanned first 60 lines)
- `Source/LinqToDB/Internal/Linq/Builder/MergeBuilder.UpdateWhenMatchedThenDelete.cs` -- no structural changes; predicate + deleteCondition pair
- `Source/LinqToDB/Internal/Linq/Builder/MinByMaxByBuilder.cs` -- new #if NET8_0_OR_GREATER builder for Queryable.MinBy/MaxBy; OrderBy+First[OrDefault] rewrite
- `Source/LinqToDB/Internal/Linq/Builder/OrderByBuilder.cs` -- CanBuildMethod rejects IComparer<TKey> overloads
- `Source/LinqToDB/Internal/Linq/Builder/ScopeContext.cs` -- Clone delegates to cloningContext.CloneContext for both Context and UpTo
- `Source/LinqToDB/Internal/Linq/Builder/SelectBuilder.cs` -- CounterContext inner class for 2-param lambda index-selector
- `Source/LinqToDB/Internal/Linq/Builder/SetOperationByBuilder.cs` -- new #if NET8_0_OR_GREATER builder for ExceptBy/UnionBy/IntersectBy; requires IsWindowFunctionsSupported
- `Source/LinqToDB/Internal/Linq/Builder/TranslationModifier.cs` -- WithIgnoreQueryFilters merges via union; sealed confirmed
- `Source/LinqToDB/Internal/Linq/Builder/Visitors/ExposeExpressionVisitor.cs` -- _isSingleConvert bool field added; cleared in Cleanup
- `Source/LinqToDB/Linq/Expressions.cs` (lines 1-80) -- NormalizeMemeberInfo helper (note: typo in source); MapMember overloads
- `Source/LinqToDB/Linq/Translation/AggregateFunctionBuilder.cs` (lines 1-80) -- _skipped bool instance field; reset at top of Build

Read (this run -- delta sha 36ee4f82f, 69 files -- diffs read in full against b3340aa9ded15; new files read in full; net stat +8177/-775 across 16 merged PRs #5482/#5450/#5525/#5661/#5468/#5630/#5604-followup/#5348/#5667/#5623/#5613/#5506/#5639 plus small maintenance touches):
- `Source/LinqToDB/Internal/Linq/Builder/AggregateExecuteBuilder.cs` -- sub-sequence build wrapped in IsolateOrderBy() (CteUnion isolation); otherwise unchanged from prior delta
- `Source/LinqToDB/Internal/Linq/Builder/AggregationBuilder.cs` -- unused-using cleanup only, no behavior change
- `Source/LinqToDB/Internal/Linq/Builder/AllAnyBuilder.cs` -- sub-sequence build wrapped in IsolateOrderBy()
- `Source/LinqToDB/Internal/Linq/Builder/AssociationHelper.cs` -- parentAlreadyFiltered param added; discriminator predicate OR-combines every inheritance code for parentType, root resolved via parentType.BaseType walk (PR #5661)
- `Source/LinqToDB/Internal/Linq/Builder/BuildContextBase.cs` -- new virtual SetElementSelection<T>(Query<T>) no-op hook
- `Source/LinqToDB/Internal/Linq/Builder/BuildContextDebuggingHelper.cs` -- reads tc.NamedTable.SourceID instead of tc.SqlTable.SourceID
- `Source/LinqToDB/Internal/Linq/Builder/CteContext.cs` -- fields backed by SqlCteField; RegisterField reorders CteClause.Fields to match inner-query column order; new RegisterVirtualField/GetFieldPlaceholder for CteUnion
- `Source/LinqToDB/Internal/Linq/Builder/CteTableContext.cs` -- NamedTable maps to CteTable; RegisterVirtualField delegates to CteContext; RemapFields handles SqlCteField/SqlCteTableField underlying types
- `Source/LinqToDB/Internal/Linq/Builder/DeleteBuilder.cs` -- deleteStatement.Table now NamedTable; Output guarded to only SqlTable targets (errors otherwise)
- `Source/LinqToDB/Internal/Linq/Builder/DistinctBuilder.cs` -- drops captured OrderBy entries not surviving the Distinct projection (CteUnion isolation, PR #5450)
- `Source/LinqToDB/Internal/Linq/Builder/DistinctByBuilder.cs` -- adds DISTINCT ON path (IsDistinctOnSupported, PR #5630); switches to captured OrderBy state via IsolateOrderBy instead of re-extracting
- `Source/LinqToDB/Internal/Linq/Builder/EagerLoadFallbackReason.cs` (new) -- diagnostic fallback-reason enum for eager-load strategy dispatch (PR #5450)
- `Source/LinqToDB/Internal/Linq/Builder/EntityBuilderConfig.cs` (new) -- parsed configure-chain state shared by entity Insert/Update/Upsert builders (PR #5482)
- `Source/LinqToDB/Internal/Linq/Builder/EntityBuilderParser.cs` (new) -- walks the configure-lambda method chain into EntityBuilderConfig (PR #5482)
- `Source/LinqToDB/Internal/Linq/Builder/EntityConstructorBase.cs` -- sharedColumns de-dup cache for TPH sibling columns; per-subtype constructor for abstract intermediates (PR #5661); ExplicitEagerLoad marker tagging when ImplicitCollectionLoading=Throw
- `Source/LinqToDB/Internal/Linq/Builder/EntityInsertBuilder.cs` (new) -- entity-builder Insert overload (PR #5482)
- `Source/LinqToDB/Internal/Linq/Builder/EntitySetterBuilder.cs` (new) -- shared setter-lambda builder for Insert/Update/Upsert (PR #5482)
- `Source/LinqToDB/Internal/Linq/Builder/EntityUpdateBuilder.cs` (new) -- entity-builder Update overload (PR #5482)
- `Source/LinqToDB/Internal/Linq/Builder/EnumerableBuilder.cs` -- relaxes Parent==null guard on NewArrayExpression to support UpsertBuilder MERGE Using(new[]{item})
- `Source/LinqToDB/Internal/Linq/Builder/EnumerableContextDynamic.cs` -- dedup key now uses the projected member own column descriptor instead of ambient currentDescriptor (fixes a duplicate-vs-mis-grouped VALUES column bug)
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuildVisitor.cs` -- TryConvertPredicate gives a MemberTranslator the first shot (#5347/#5348); PreferClientCalculation gating; enum-vs-null IS NULL fix (#5666/#5667); NamedTable rename; IsolateOrderBy around HandleSubquery; parentAlreadyFiltered wiring; new ProviderFlags property; removed a stale placeholder-skip guard in TranslateMember
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.Associations.cs` -- IOrderedEnumerable<T> adaptation now wraps with PassThroughOrderedCollection<T> instead of returning the source unchanged
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.EagerLoad.cs` -- now the strategy dispatcher (CompleteEagerLoadingExpressions/ResolveStrategy) plus shared helpers (GenerateKeyExpression off-by-one fix, CollectOrderBy/RemapOrderByThroughSelect, ValueTuple 8+ nesting helpers, ReplaceLeakedSubqueries); the old Default-strategy body was extracted out to EagerLoadDefault.cs
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.EagerLoadDefault.cs` (new) -- Default eager-load strategy body, extracted near-verbatim from the pre-delta EagerLoad.cs (PR #5450)
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.EagerLoadKeyedQuery.cs` (new, 1472 lines -- structural read: class/method signatures plus the entry-point body and key-holder/preamble types; not every helper line traced) -- KeyedQuery strategy: buffer main results, extract distinct parent keys, single batched child query (PR #5450)
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.EagerLoadState.cs` (new) -- EagerLoadState/LastEagerLoadFallbackChain scratch state (PR #5450)
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.EagerLoadUnion.cs` (new, 1828 lines -- structural read: class/method signatures plus the entry-point body and carrier/branch types; not every helper line traced) -- CteUnion strategy: batch eager loads into one UNION ALL carrier query (PR #5450)
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.QueryBuilder.cs` -- FinalizeProjection now runs ImplicitEagerLoadGuardVisitor (guarded by _inEagerLoadProcessing) and applies a strategy-supplied finalizer instead of unconditionally calling ToColumns
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.SqlBuilder.cs` -- adds an IsServerSideOnly passthrough; PushDisabledQueryFilters(string[],Type[]) overload and IsFilterDisabled(Type,string) for named filters; NamedTable rename
- `Source/LinqToDB/Internal/Linq/Builder/ExpressionBuilder.cs` -- adds _currentOrderBy/RegisterOrderBy/IsolateOrderBy/ResolveOrderByItems/RemoveOrderByEntries; TranslationProviderFlags cached property; BuildQuery routes to SetRunQueryWithCteUnion/KeyedQueryBuffer based on the committed EagerLoadState; skips re-Finalize when query.IsFinalized
- `Source/LinqToDB/Internal/Linq/Builder/FilterIgnoreScope.cs` (new) -- two-dimensional key/type ignore-scope for named query filters (PR #5525)
- `Source/LinqToDB/Internal/Linq/Builder/FirstSingleBuilder.cs` -- SetRunQuery now calls a new SetElementSelection step separately
- `Source/LinqToDB/Internal/Linq/Builder/GroupJoinBuilder.cs` -- cosmetic whitespace only
- `Source/LinqToDB/Internal/Linq/Builder/IBuildContext.cs` -- new SetElementSelection<T>(Query<T>) interface member
- `Source/LinqToDB/Internal/Linq/Builder/ITableContext.cs` -- SqlTable replaced by NamedTable (ISqlNamedTable)
- `Source/LinqToDB/Internal/Linq/Builder/IgnoreFiltersBuilder.cs` -- second dispatch for IgnoreFiltersByKey(keys,types) (PR #5525)
- `Source/LinqToDB/Internal/Linq/Builder/ImplicitEagerLoadGuardVisitor.cs` (new) -- throws on an implicit (unmarked) SqlEagerLoadExpression when ImplicitCollectionLoading=Throw (PR #5450)
- `Source/LinqToDB/Internal/Linq/Builder/InsertBuilder.cs` -- CanBuildMethod excludes the entity-builder Insert shape
- `Source/LinqToDB/Internal/Linq/Builder/InsertOrUpdateBuilder.cs` -- SetRunQuery reuses UpsertBuilder.HasDivergentInsertOrUpdateBranches/MatchKeysDivergeFromPrimaryKey for the SAP HANA emulation decision
- `Source/LinqToDB/Internal/Linq/Builder/LoadWithBuilder.cs` -- [BuildsMethodCall] list gains "WithEagerLoadingStrategy"
- `Source/LinqToDB/Internal/Linq/Builder/MergeBuilder.cs` -- ReplaceSourceInQuery/GetTargetTable widened to ISqlTableSource/SqlCteTable to support CTE merge targets
- `Source/LinqToDB/Internal/Linq/Builder/MethodChainBuilder.cs` -- null-check wrapping skipped when flags.IsForSetProjection()
- `Source/LinqToDB/Internal/Linq/Builder/OfTypeBuilder.cs` -- calls table.AddFilteredByOfType(objectType) to record TPH narrowing for association discriminator skip
- `Source/LinqToDB/Internal/Linq/Builder/OrderByBuilder.cs` -- calls builder.RegisterOrderBy after adding each OrderBy/ThenBy clause (CteUnion support)
- `Source/LinqToDB/Internal/Linq/Builder/ParametersContext.cs` -- BuildParameter now rewrites [DynamicColumnsStore] member accesses via ReplaceDynamicColumnGetters before parameter allocation
- `Source/LinqToDB/Internal/Linq/Builder/QueryExtensionBuilder.cs` -- uses GetTableContext instead of GetTableOrCteContext for hint attachment
- `Source/LinqToDB/Internal/Linq/Builder/RecordReaderBuilder.cs` -- fast-mode Reduce of ConvertFromDataReaderExpression (PR #5659); duplicate-column-name-safe reader index build; OptimizeForSequentialAccess now read from DataOptions.LinqOptions (PR #5639)
- `Source/LinqToDB/Internal/Linq/Builder/ScalarSelectBuilder.cs` -- ScalarSelectContext.IsSingleElement=true, DoNotRemove set in constructor (survives Clone); promotes the body to a (SELECT body) scalar-subquery column when built for SQL, keeping a window ORDER BY key valid on SQL Server
- `Source/LinqToDB/Internal/Linq/Builder/SequenceHelper.cs` -- adds IsSpecialProperty(Expression,IBuildContext)/ChangeSpecialPropertyObject overloads (CteUnion virtual-field support)
- `Source/LinqToDB/Internal/Linq/Builder/SetOperationBuilder.cs` -- each set-operation side build wrapped in IsolateOrderBy(); MakeExpression additionally passes through flags.IsSubquery()
- `Source/LinqToDB/Internal/Linq/Builder/SubQueryContext.cs` -- MakeExpression pass-through condition reorders the flags.IsSubquery() check (no material change)
- `Source/LinqToDB/Internal/Linq/Builder/TableBuilder.RawSqlContext.cs` -- reads NamedTable instead of SqlTable
- `Source/LinqToDB/Internal/Linq/Builder/TableBuilder.TableContext.cs` -- NamedTable maps to SqlTable; new FilteredByOfType/AddFilteredByOfType (TPH narrowing tracking, cloned across Clone()); SetAlias member match now also compares PhysicalName
- `Source/LinqToDB/Internal/Linq/Builder/TableBuilder.cs` -- ApplyQueryFilters rewritten for EntityDescriptor.QueryFilters (a list) with per-key active-filter selection and the new BuildFilterLambda helper (PR #5525)
- `Source/LinqToDB/Internal/Linq/Builder/TableLikeHelpers.cs` -- new GenerateColumnAlias(Expression?, ISqlExpression) overload preferring a letter/digit/underscore path alias, falling back to the SQL-expression-derived alias
- `Source/LinqToDB/Internal/Linq/Builder/TranslationModifier.cs` -- IgnoreQueryFilters (Type[]) replaced by IgnoreQueryFilterScopes (FilterIgnoreScope[]); new EagerLoadingStrategy? field; WithIgnoreQueryFilterScope/IsFilterDisabled(Type,string) additions (PR #5525/#5450)
- `Source/LinqToDB/Internal/Linq/Builder/UpdateBuilder.cs` -- CanBuildMethod excludes the entity-builder Update shape; NeedsConversion widened to SqlFieldBase; GetTableOrCteContext replaced by GetTableContext in UPDATE-FROM join detection; CreateDeletedInsertedContexts throws on an unexpected table-context type instead of silently defaulting
- `Source/LinqToDB/Internal/Linq/Builder/UpsertBuilder.cs` (new) -- the full fluent Upsert implementation, native + MERGE-lowering paths (PR #5482)
- `Source/LinqToDB/Internal/Linq/Builder/Visitors/ExposeExpressionVisitor.cs` -- IMemberConverter.Convert calls now pass a new IConvertContext; drops the F# block-simplification VisitBlock override
- `Source/LinqToDB/Internal/Linq/Builder/WithLoadStrategyBuilder.cs` (new) -- WithUnionLoadStrategy/WithSeparateLoadStrategy/WithKeyedLoadStrategy marker builder (PR #5450)
- `Source/LinqToDB/Linq/Expressions.cs` -- Members/_binaries dictionaries become ConcurrentDictionary (PR #5623 thread safety); the local string.CompareTo mapping was removed (migrated to a member translator, issue #5613)
- `Source/LinqToDB/Linq/IEntityInsertSpec.cs` (new) -- public Insert-builder fluent interface (PR #5482)
- `Source/LinqToDB/Linq/IEntityUpdateSpec.cs` (new) -- public Update-builder fluent interface (PR #5482)
- `Source/LinqToDB/Linq/IUpsertInsertSpec.cs` (new) -- Upsert INSERT-branch fluent interface (PR #5482)
- `Source/LinqToDB/Linq/IUpsertSpec.cs` (new) -- root Upsert fluent interface (PR #5482)
- `Source/LinqToDB/Linq/IUpsertUpdateSpec.cs` (new) -- Upsert UPDATE-branch fluent interface (PR #5482)
- `Source/LinqToDB/Linq/Translation/ITranslationContext.cs` -- adds a ProviderFlags property (TranslationProviderFlags, PR #5468)
- `Source/LinqToDB/Linq/Translation/TranslationProviderFlags.cs` (new) -- read-only SqlProviderFlags subset for translators (PR #5468)
- `Source/LinqToDB/Linq/Translation/WindowFunctionsMemberTranslator.cs` (structural read: full method-signature inventory via grep, plus targeted reads of the class doc and dispatch helpers; individual method bodies not all traced) -- rewritten/expanded to about 50 virtual Translate* methods for the new Sql.Window API (PR #5468)

- Tier 3 (skipped, logged): 0
</details>
