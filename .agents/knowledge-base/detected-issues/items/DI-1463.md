---
area: TESTS-LINQ
kind: detected-issue
sources: [code]
confidence: high
last_verified: 2026-07-06
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
---

# DI-1463 -- TODO: TPH nullable-metadata asserts may be incorrect pending fixed model (TphInheritanceTests)

**Severity**: low  **Category**: todo-fixme  **Status**: open

## Location
- `Tests/Linq/Linq/TphInheritanceTests.cs:1691`

## Pattern matched
// TODO: those asserts could be incorrect, depends on our fixed model inside TPH_Intermediate_CreateTableNullableFields, directly following the same-issue TODOs at lines 1600/1606/1612 (DI-1462). Same lineage as the previously-tracked DI-0474 in InheritanceTests.cs before the TPH region moved into this new file this delta.

## Why it matters
The test author flags that its CanBeNull assertions are provisional and may need to change once linq2db/linq2db#4364 is fixed -- a green run today does not guarantee the assertions will still be correct after the fix.

## Suggested fix
Revisit both this comment and DI-1462 together once #4364 lands; update the asserted CanBeNull expectations to match the fixed model.

## Related GitHub items
- #4364 -- Referencing intermediate child classes of an abstract class using Linq2Db single table inheritance.

## Excerpt
```csharp
			tb.Insert(() => new CreateTable1() { Id = 1, Type = 1, Field1 = 1});
			tb.Insert(() => new CreateTable2() { Id = 2, Type = 2, Field2 = 2});

			// TODO: those asserts could be incorrect, depends on our fixed model
			var column = db.MappingSchema.GetEntityDescriptor(typeof(CreateTable1)).Columns.Single(c => c.ColumnName == "Field1");
			Assert.That(column.CanBeNull, Is.False);
```
