---
area: CORE
kind: detected-issue
sources: [code]
confidence: high
last_verified: 2026-07-06
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
---

# DI-1281 -- TODO/FIXME/HACK/XXX comment

**Severity**: low  **Category**: todo-fixme  **Status**: open

## Location
- `Source/LinqToDB/LinqOptions.cs:295` -- binary-compat overload of the positional record's Deconstruct method, marked for removal in v7

## Pattern matched
`// TODO` comment (case-insensitive, space after `//`) -- matches the todo-fixme pattern catalog entry. Full comment: "// TODO: remove in v7 (binary-compat shim -- drop together with the matching constructor overload)."

## Why it matters
Sibling of DI-1280: this marks removal of the Deconstruct overload that mirrors the pre-#5482 15-parameter shape. The two shims exist as a pair (constructor + Deconstruct) so a with-expression or positional-pattern match compiled against an older linq2db release keeps working; if one is removed without the other at the v7 cutover, callers still targeting the old shape would break inconsistently (construct but not deconstruct, or vice versa).

## Suggested fix
Remove together with DI-1280's constructor overload when the v7 major bump drops binary compatibility with pre-#5482 assemblies.

## Related GitHub items
None found in the current open-issues snapshot.

## Excerpt
```csharp
  291: 		/// <summary>
  292: 		/// Binary-compatibility overload of the record's <c>Deconstruct</c> -- mirrors the
  293: 		/// method signature as it was before <see cref="UpsertEmulationPolicy"/> and the later options were added.
  294: 		/// </summary>
> 295: 		// TODO: remove in v7 (binary-compat shim -- drop together with the matching constructor overload).
  296: 		[EditorBrowsable(EditorBrowsableState.Never)]
  297: 		public void Deconstruct(
  298: 			out bool         preloadGroups,
  299: 			out bool         ignoreEmptyUpdate,
  300: 			out bool         generateExpressionTest,
  301: 			out bool         traceMapperExpression,
  302: 			out bool         concatenateOrderBy,
  303: 			out bool         optimizeJoins,
  304: 			out CompareNulls compareNulls,
  305: 			out bool         guardGrouping,
```
