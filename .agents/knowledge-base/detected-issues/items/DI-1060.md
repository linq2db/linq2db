---
area: PROV-SYBASE
kind: detected-issue
sources: [code]
confidence: high
last_verified: 2026-06-15
last_verified_sha: b3340aa9ded15ffc626983fd202e6399daa081ca
---

# DI-1060 — SybaseOptions public record missing <summary> XML doc

**Severity**: low  **Category**: doc-gap  **Status**: open

## Location
- `Source/LinqToDB/DataProvider/Sybase/SybaseOptions.cs:16` — `public sealed record SybaseOptions` has a `<param>` block but no `<summary>` tag

## Pattern matched
Public API type without `<summary>` XML doc (Tier-1 file in PROV-SYBASE area). The record carries a `/// <param name="BulkCopyType">` doc comment but the type-level `<summary>` is absent. The `doc-gap` pattern fires on any public type in a Tier-1 file that lacks a `<summary>`.

## Why it matters
Consumers browsing IntelliSense or the generated API reference see no description for `SybaseOptions` — only the parameter detail. Any IDE hover over `new SybaseOptions()` or `SybaseOptions.Default` shows an empty type-level tooltip. Future maintainers adding parameters must guess the record's overall purpose from context. As the options type grows this becomes harder to navigate.

## Suggested fix
Add a `<summary>` above the existing `<param>` block, e.g.:

```xml
/// <summary>
/// Provider-specific options for the Sybase ASE data provider.
/// </summary>
/// <param name="BulkCopyType">
/// ...
/// </param>
```

## Related GitHub items
(none)

## Excerpt
```csharp
 8	namespace LinqToDB.DataProvider.Sybase
 9	{
10		/// <param name="BulkCopyType">
11		/// Using <see cref="BulkCopyType.ProviderSpecific"/> mode with bit and identity fields could lead to following errors:
12		/// - bit: <see langword="false"/> inserted into bit field for first record even if <see langword="true"/> provided;
13		/// - identity: bulk copy operation fail with exception: "Bulk insert failed. Null value is not allowed in not null column.".
14		/// Those are provider bugs and could be fixed in latest versions.
15		/// </param>
16		public sealed record SybaseOptions
```
