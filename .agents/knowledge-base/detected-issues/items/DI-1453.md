---
area: TESTS-LINQ
kind: detected-issue
sources: [code]
confidence: medium
last_verified: 2026-07-06
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
---

# DI-1453 -- ActiveIssue-gated tests without issue reference: TestInsertIssue1/2 (InheritanceTests)

**Severity**: low  **Category**: broken-test  **Status**: open

## Location
- `Tests/Linq/Linq/InheritanceTests.cs:1298`
- `Tests/Linq/Linq/InheritanceTests.cs:1320`

## Pattern matched
Two bare [ActiveIssue] gates on bare [Test] methods (TestInsertIssue1, TestInsertIssue2) -- no issue number or URL anywhere in the attribute, method name, or nearby comments. Merged into one entry (same file + same no-reference signature).

## Why it matters
These two tests (simple sequential Insert against a TPH base class) are permanently skipped with zero traceability to why -- a future contributor cannot tell if the underlying bug was ever filed, fixed, or is still real.

## Suggested fix
Either file a GitHub issue documenting the failure and add its Description URL, or if the underlying bug is already fixed, remove the gate and confirm the tests pass.

## Related GitHub items
- (none)

## Excerpt
```csharp
		[ActiveIssue]
		[Test]
		public void TestInsertIssue1([DataSources] string context)
		{
			using var db = GetDataContext(context);
			using var _ = db.CreateLocalTable<BaseClass>();
			db.GetTable<BaseClass>().Value(_ => _.Id, 1).Insert();
			...
		}

		[ActiveIssue]
		[Test]
		public void TestInsertIssue2([DataSources] string context)
```
