---
area: EXPR-TRANS
kind: detected-issue
sources: [code]
confidence: high
last_verified: 2026-07-06
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
---

# DI-1330 -- TODO/FIXME/HACK/XXX comment

**Severity**: low  **Category**: todo-fixme  **Status**: open

## Location
- `Source/LinqToDB/Internal/Linq/Builder/IBuildContext.cs:21` -- `Parent` property getter/setter on the `IBuildContext` interface

## Pattern matched
`// TODO` comment (todo-fixme catalog entry: `// TODO`, `// FIXME`, `// HACK`, `// XXX`, case-insensitive). The comment predates this diff -- the file entered this delta's scope because the same interface gained `SetElementSelection<T>` two lines below (part of the eager-loading rewrite) -- but it was never previously tracked in the detected-issues index.

## Why it matters
`IBuildContext.Parent` is a settable property every build-context implementer (20+ classes under `Internal/Linq/Builder/`) must satisfy. The TODO signals nobody has confirmed whether it is still load-bearing; leaving it unresolved means every new context type (this delta alone adds several: `EntityInsertBuilder`, `EntityUpdateBuilder`, the `EagerLoad*` contexts) keeps wiring a property of unverified necessity, and removing it later would be an interface-wide change touching every implementer at once.

## Suggested fix
Grep for `.Parent` reads/writes across `Internal/Linq/Builder/**` to confirm live call sites. If the only remaining use is incidental, remove the property from the interface and all implementers; otherwise replace the TODO with a comment stating what still depends on it, so the ambiguity does not recur on the next pass.

## Related GitHub items
- none found

## Excerpt
```csharp
17			ExpressionBuilder   Builder             { get; }
18			MappingSchema       MappingSchema       { get; }
19			Expression?         Expression          { get; }
20			SelectQuery         SelectQuery         { get; }
21			IBuildContext?      Parent              { get; set; } // TODO: probably not needed
22			TranslationModifier TranslationModifier { get; }
23	
24			SelectQuery GetResultQuery();
```
