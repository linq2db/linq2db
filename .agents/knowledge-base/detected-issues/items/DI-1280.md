---
area: CORE
kind: detected-issue
sources: [code]
confidence: high
last_verified: 2026-07-06
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
---

# DI-1280 -- TODO/FIXME/HACK/XXX comment

**Severity**: low  **Category**: todo-fixme  **Status**: open

## Location
- `Source/LinqToDB/LinqOptions.cs:264` -- binary-compat overload of the positional record constructor, marked for removal in v7

## Pattern matched
`// TODO` comment (case-insensitive, space after `//`) -- matches the todo-fixme pattern catalog entry. Full comment: "// TODO: remove in v7 (binary-compat shim -- drop together with the matching Deconstruct overload)."

## Why it matters
This marks a scheduled removal for the 15-parameter LinqOptions constructor overload, added to keep assemblies compiled before UpsertEmulationPolicy / DefaultEagerLoadingStrategy / ImplicitCollectionLoading / OptimizeForSequentialAccess were added to the record loading correctly. Low severity in isolation, but it is a live delete-in-v7 tracker paired with DI-1281 (the matching Deconstruct overload); if v7 planning does not sweep TODO markers before cutting the major version, this compatibility shim and its sibling will linger past their intended removal window as unnecessary public surface.

## Suggested fix
When the v7 major version bump lands and binary compatibility with pre-#5482 assemblies is no longer required, delete this constructor overload together with the paired Deconstruct overload (DI-1281) and this comment.

## Related GitHub items
None found in the current open-issues snapshot.

## Excerpt
```csharp
  259: 		/// <summary>
  260: 		/// Binary-compatibility overload of the record's positional constructor -- mirrors the
  261: 		/// public ctor signature as it was before <see cref="UpsertEmulationPolicy"/> and the later options were added,
  262: 		/// so assemblies compiled against the previous linq2db release continue to load.
  263: 		/// </summary>
> 264: 		// TODO: remove in v7 (binary-compat shim -- drop together with the matching Deconstruct overload).
  265: 		[EditorBrowsable(EditorBrowsableState.Never)]
  266: 		public LinqOptions(
  267: 			bool         preloadGroups,
  268: 			bool         ignoreEmptyUpdate,
  269: 			bool         generateExpressionTest,
  270: 			bool         traceMapperExpression,
  271: 			bool         concatenateOrderBy,
  272: 			bool         optimizeJoins,
  273: 			CompareNulls compareNulls,
```
