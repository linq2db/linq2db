---
area: TESTS-LINQ
kind: detected-issue
sources: [code]
confidence: high
last_verified: 2026-07-06
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
---

# DI-1460 -- TODO: UNION ALL optimization not yet asserted as single query (EagerLoadingStrategyUnionTests, 3 methods)

**Severity**: low  **Category**: todo-fixme  **Status**: open

## Location
- `Tests/Linq/Linq/EagerLoadingStrategyUnionTests.cs:1129`
- `Tests/Linq/Linq/EagerLoadingStrategyUnionTests.cs:1172`
- `Tests/Linq/Linq/EagerLoadingStrategyUnionTests.cs:1209`

## Pattern matched
Identical // TODO: When full UNION ALL optimization is implemented, assert counter.Count == 1 comment at the end of three empty-master eager-loading test methods. Merged into one entry (same file plus identical TODO text).

## Why it matters
The UNION-load-strategy tests currently only assert the empty-result-set shape, not the query-count optimization the strategy is supposed to deliver; a regression that silently reverts to N+1 queries would not be caught here.

## Suggested fix
Once the full UNION ALL optimization lands, add counter.Count.ShouldBe(1) assertions to all three methods and remove the TODOs.

## Related GitHub items
- (none)

## Excerpt
```csharp
			var result = query
				.WithUnionLoadStrategy()
				.ToList();

			result.Count.ShouldBe(0);

			// TODO: When full UNION ALL optimization is implemented, assert counter.Count == 1
		}
```
