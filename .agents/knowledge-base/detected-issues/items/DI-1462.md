---
area: TESTS-LINQ
kind: detected-issue
sources: [code]
confidence: high
last_verified: 2026-07-06
last_verified_sha: 36ee4f82f06eaf242b052ade8c87121d251a6165
---

# DI-1462 -- TODO: TPH deep-hierarchy nullable fields workaround pending fix (TphInheritanceTests, 3 fields)

**Severity**: low  **Category**: todo-fixme  **Status**: open

## Location
- `Tests/Linq/Linq/TphInheritanceTests.cs:1600`
- `Tests/Linq/Linq/TphInheritanceTests.cs:1606`
- `Tests/Linq/Linq/TphInheritanceTests.cs:1612`

## Pattern matched
Identical // TODO: remove when fixed, nullable added due to TPH_Intermediate_CreateTableNullableFields comment on three [LinqToDB.Mapping.Nullable]-decorated fields. Merged into one entry (same file plus identical TODO text) -- this is the same debt statement previously tracked as DI-0471/0472/0473 in Tests/Linq/Linq/InheritanceTests.cs before the TPH region moved into this new file this delta (see the paired fixed-status update for those three IDs).

## Why it matters
Three test-model fields are marked nullable purely as a workaround for the CanBeNull metadata issue tracked in linq2db/linq2db#4364 -- if that ships, three call sites need the [Nullable] workaround removed together, or the TPH nullable-column model diverges from the intended non-nullable shape.

## Suggested fix
Once linq2db/linq2db#4364 (CreateTable nullable-metadata handling) is fixed, remove [LinqToDB.Mapping.Nullable] from all three fields and confirm TPH_Intermediate_CreateTableNullableFields still passes.

## Related GitHub items
- #4364 -- Referencing intermediate child classes of an abstract class using Linq2Db single table inheritance.

## Excerpt
```csharp
		class TphThingBeta : TphThingBase
		{
			// TODO: remove when fixed, nullable added due to TPH_Intermediate_CreateTableNullableFields
			[LinqToDB.Mapping.Nullable] public int ConcreteField { get; set; }
		}

		abstract class TphThingIntermediate : TphThingBase
		{
			// TODO: remove when fixed, nullable added due to TPH_Intermediate_CreateTableNullableFields
			[LinqToDB.Mapping.Nullable] public int IntermediateField { get; set; }
		}

		class TphThingIntermediateOne : TphThingIntermediate
		{
			// TODO: remove when fixed, nullable added due to TPH_Intermediate_CreateTableNullableFields
			[LinqToDB.Mapping.Nullable] public int ConcreteField { get; set; }
		}
```
