# build solution only
variables:
  - template: templates/build-vars.yml

# always trigger on PRs
trigger: none
pr:
  branches:
    include:
    - '*'

stages:

- stage: ''
  displayName: ''
  jobs:
##############
#  BUILD JOB #
##############
  - template: templates/build-job.yml
    parameters:
      with_nugets: true
      with_examples: true
      with_tests: false
      with_analyzers: true
      with_release: false
