variables:
# also check hardcoded branch names in pipeline triggers and nuget-vars.yml
  master_branch: master
  release_branch: release_v4
  baselines_master: master
  solution: 'linq2db.sln'
  build_configuration: 'Azure'
  assemblyVersion: 4.4.1
  ${{ if eq(variables['Build.SourceBranchName'], 'release_v4') }}:
    packageVersion: 4.4.1
  ${{ if ne(variables['Build.SourceBranchName'], 'release_v4') }}:
    packageVersion: 4.4.1
