variables:
# also check hardcoded branch names in pipeline triggers and nuget-vars.yml
  master_branch: dev.v4
  release_branch: release4
  baselines_master: master.4
  solution: 'linq2db.sln'
  build_configuration: 'Azure'
  assemblyVersion: 4.0.0
  ${{ if eq(variables['Build.SourceBranchName'], 'release4') }}:
    packageVersion: 4.0.0
  ${{ if ne(variables['Build.SourceBranchName'], 'release4') }}:
    packageVersion: 4.0.0
