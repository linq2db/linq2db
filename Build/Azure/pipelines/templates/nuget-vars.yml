variables:
  ${{ if eq(variables['Build.SourceBranchName'], $[variables['release_branch']]) }}:
    packageVersion: $(nugetVersion)
  ${{ if eq(variables['Build.SourceBranchName'], $[variables['master_branch']]) }}:
    packageVersion: $(nugetDevVersion)
