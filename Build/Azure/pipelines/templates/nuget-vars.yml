variables:
  ${{ if eq(variables['Build.SourceBranchName'], 'release') }}:
    packageVersion: $(nugetVersion)
  ${{ if eq(variables['Build.SourceBranchName'], 'master') }}:
    packageVersion: $(nugetDevVersion)
  ${{ if eq(variables.publish, false) }}:
    packageVersion: 1.0.0
