---
id: DI-0494
kind: detected-issue
area: TESTS-LINQ
category: todo-fixme
severity: low
status: open
sources: [code]
confidence: high
first_detected: 2026-05-08
first_detected_sha: d52a4ed0e83317cc11b2ef3d41ddf4ba70acf415
last_seen: 2026-05-08
last_seen_sha: d52a4ed0e83317cc11b2ef3d41ddf4ba70acf415
pattern: todo-comment
---

# DI-0494 — TODO/FIXME/HACK/XXX comment

**File:** `Tests/Linq/SchemaProvider/PostgreSQLSchemaProviderTests.cs:512`

**Matched:** `// TODO`

## Context

```csharp
  507: 				Assert.That(view.Columns[0].SystemType, Is.EqualTo(typeof(int)));
  508: 				Assert.That(view.Columns[0].DataType, Is.EqualTo(DataType.Int32));
  509: 				Assert.That(view.Columns[0].SkipOnInsert, Is.True);
  510: 				Assert.That(view.Columns[0].SkipOnUpdate, Is.True);
  511: 				Assert.That(view.Columns[0].Length, Is.Null);
> 512: 				// TODO: maybe we should fix it?
  513: 				Assert.That(view.Columns[0].Precision, Is.EqualTo(32));
  514: 				Assert.That(view.Columns[0].Scale, Is.Zero);
  515: 				Assert.That(view.Columns[0].Table, Is.EqualTo(view));
  516: 
  517: 				Assert.That(view.Columns[1].ColumnName, Is.EqualTo("FirstName"));
```

## Pattern

Detected by: `todo-comment`
Category: todo-fixme (default severity: low)

See [kb-issue-categories.md](../../../docs/kb-issue-categories.md) for category definitions.
