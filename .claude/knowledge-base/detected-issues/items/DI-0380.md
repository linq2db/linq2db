---
id: DI-0380
kind: detected-issue
area: TESTS-LINQ
category: todo-fixme
severity: low
status: open
sources: [code]
confidence: high
first_detected: 2026-05-08
first_detected_sha: d52a4ed0e83317cc11b2ef3d41ddf4ba70acf415
last_seen: 2026-05-08
last_seen_sha: d52a4ed0e83317cc11b2ef3d41ddf4ba70acf415
pattern: todo-comment
---

# DI-0380 — TODO/FIXME/HACK/XXX comment

**File:** `Tests/Linq/DataProvider/Types/ClickHouseTypeTests.cs:289`

**Matched:** `// TODO`

## Context

```csharp
  284: 			await TestType<Guid, Guid?>(context, new(typeof(Guid)), default, default);
  285: 			await TestType<Guid, Guid?>(context, new(typeof(Guid)), TestData.Guid1, TestData.Guid2);
  286: 
  287: 			await TestType<Guid, Guid?>(context, new(typeof(Guid), DataType.Guid), default, default);
  288: 			await TestType<Guid, Guid?>(context, new(typeof(Guid), DataType.Guid), TestData.Guid1, TestData.Guid2);
> 289: 
  290: 			// TODO: disabled for now, as currently mapper expression doesn't know about DataType to generate correct mapping
  291: 			// workaround possible but it will be better to add DataType/DbDataType support to reader expressions
  292: 			//await TestType<Guid, Guid?>(context, new(typeof(Guid), DataType.Binary, null, length: 16), default, default);
  293: 			//await TestType<Guid, Guid?>(context, new(typeof(Guid), DataType.Binary, null, length: 16), TestData.Guid1, TestData.Guid2);
  294: 
```

## Pattern

Detected by: `todo-comment`
Category: todo-fixme (default severity: low)

See [kb-issue-categories.md](../../../docs/kb-issue-categories.md) for category definitions.
