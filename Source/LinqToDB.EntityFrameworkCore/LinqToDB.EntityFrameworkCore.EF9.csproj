<Project Sdk="Microsoft.NET.Sdk">
	
	<Import Project="LinqToDB.EntityFrameworkCore.props" />

	<PropertyGroup>
		<TargetFrameworks>net8.0</TargetFrameworks>
		<Version>$(EF9Version)</Version>
		<AssemblyVersion>$(EF9Version)</AssemblyVersion>
		<DefineConstants>EF9;$(DefineConstants)</DefineConstants>
	</PropertyGroup>

	<ItemGroup>
		<PackageReference Update="Microsoft.EntityFrameworkCore.Relational" VersionOverride="9.0.0" />
	</ItemGroup>

	<PropertyGroup Label="Nuget">
		<PackageId>linq2db.EntityFrameworkCore</PackageId>
		<Title>Linq to DB extensions for Entity Framework Core 9</Title>
		<Description>Allows to execute Linq to DB queries in Entity Framework Core DbContext.</Description>
		<Product>$(Title)</Product>

		<PackageVersion Condition=" '$(ApplyVersionSuffix)' == 'true' ">$(Version)$(VersionSuffix)</PackageVersion>
		<PackageValidationBaselineVersion>9.1.0</PackageValidationBaselineVersion>

		<PackageTags>$(PackageTags);entity-framework-core;EntityFrameworkCore;EFCore</PackageTags>
	</PropertyGroup>

	<ItemGroup Label="Nuget">
		<None Include="readme.md" Pack="true" PackagePath="readme.md" />
	</ItemGroup>

</Project>
