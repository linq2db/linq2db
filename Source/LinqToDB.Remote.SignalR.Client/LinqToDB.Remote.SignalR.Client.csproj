<Project Sdk="Microsoft.NET.Sdk">
	<PropertyGroup>
		<AssemblyName>linq2db.Remote.SignalR.Client</AssemblyName>
		<RootNamespace>LinqToDB.Remote.SignalR</RootNamespace>

		<IsAotCompatible Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net8.0'))">true</IsAotCompatible>
	</PropertyGroup>

	<PropertyGroup Label="Nuget">
		<Title>LINQ to DB remote data context over Signal/R</Title>
		<Description>LINQ to DB Remote Signal/R package provides client classes to query database remotely using Signal/R.</Description>
		<Product>$(Title)</Product>

		<PackageTags>$(PackageTags);Signal/R;RemoteLinq</PackageTags>
	</PropertyGroup>

	<ItemGroup Label="Nuget">
		<None Include="readme.md" Pack="true" PackagePath="readme.md" />
	</ItemGroup>

	<ItemGroup>
		<ProjectReference Include="..\LinqToDB\LinqToDB.csproj" />

		<PackageReference Include="Microsoft.AspNetCore.SignalR.Client" />
	</ItemGroup>

	<ItemGroup Condition="!$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net8.0'))">
		<!--transitive vulnerable packages-->
		<PackageReference Include="Newtonsoft.Json" />
	</ItemGroup>

	<ItemGroup>
		<AdditionalFiles Include="PublicAPI.*.txt" />
	</ItemGroup>
</Project>
